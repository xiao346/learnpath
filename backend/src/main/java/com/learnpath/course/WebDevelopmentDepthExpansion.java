package com.learnpath.course;

import java.util.List;
import java.util.Map;

/** Chapter-specific second-pass concepts for the four website-building courses. */
public final class WebDevelopmentDepthExpansion {

    private WebDevelopmentDepthExpansion() {
    }

    private static final Map<String, List<String>> POINTS = Map.ofEntries(
            e("网页从哪里开始",
                    "浏览器先解析 HTML 建立 DOM，再解析 CSS 建立 CSSOM，二者结合形成渲染树，随后完成布局、绘制与合成。",
                    "相对路径以当前文档或配置的 base 地址为起点解析，目录层级变化会直接影响 CSS、脚本与图片能否找到。",
                    "开发者工具中临时修改 DOM 或样式不会自动写回源文件，刷新后消失，因此定位完成后仍要修改项目代码并重新验证。"),
            e("用 HTML 组织内容",
                    "语义元素会形成页面地标和文档结构，main 通常表示主要内容，nav 表示主要导航，标题层级表达内容的父子关系。",
                    "图片的 alt 应描述图片在当前上下文承担的信息；纯装饰图可使用空 alt，让辅助技术跳过无意义重复。",
                    "相对链接适合同一站点内可随目录迁移的页面，绝对 URL 指向确定来源；链接文字应能单独说明目的地。"),
            e("表单与语义标签",
                    "表单提交时主要发送拥有 name 的成功控件及其当前 value，仅有 id 而没有 name 的输入通常不会进入提交数据。",
                    "label 的 for 应与控件 id 对应，点击标签即可聚焦控件，也能让辅助技术把可见题目与输入框关联起来。",
                    "浏览器约束校验可以改善输入体验，却能被绕过；长度、格式、权限和业务唯一性仍必须在服务端再次检查。"),
            e("CSS 选择器与层叠",
                    "层叠结果依次受来源与重要性、级联层、选择器优先级和声明顺序影响，不能只用“后写覆盖先写”解释全部冲突。",
                    "可继承属性通常与文字有关，宽度、边框和间距通常不继承；inherit、initial、unset 表达的重置语义也不同。",
                    "伪类描述元素状态或结构位置，伪元素创建可样式化的虚拟部分；它们不会替代页面中真正需要语义的内容。"),
            e("盒模型与 Flex 布局",
                    "content-box 的声明宽度不包含内边距与边框，border-box 则包含二者；计算溢出前必须先确认采用哪种盒模型。",
                    "Flex 项目的最终尺寸由 flex-basis、可用空间、flex-grow 与 flex-shrink 共同决定，并不等于简单平均容器宽度。",
                    "长文本可能因默认 min-width:auto 阻止 Flex 项收缩，给需要收缩的子项设置 min-width:0 才能让省略与换行按预期发生。"),
            e("响应式个人主页",
                    "移动优先样式先覆盖窄屏基本体验，再用 min-width 媒体查询逐步增强，能够减少互相覆盖的断点规则。",
                    "断点应在内容开始拥挤或阅读节奏被破坏的位置设置，而不是照抄某款设备宽度；拖动视口可以直接发现临界点。",
                    "响应式还包括字体缩放、触控目标、键盘焦点和图片资源选择，页面不横向滚动只是最低验收条件。"),

            e("JavaScript 第一次登场",
                    "普通 defer 脚本会在 DOM 解析完成后按文档顺序执行，type=module 默认延迟执行并拥有独立模块作用域。",
                    "JavaScript 运行错误通常会中断当前调用栈后续语句，控制台首条错误的位置与堆栈比连锁报错更接近根因。",
                    "脚本读取元素可能得到 null，原因可能是选择器错误或执行时机过早；应分别检查 DOM 是否存在与脚本何时运行。"),
            e("变量、类型与数组",
                    "const 限制的是变量绑定不能重新指向别处，并不冻结对象或数组内部；需要不可变更新时应创建新对象或新数组。",
                    "原始值按值复制，对象变量保存引用；两个内容相同的对象使用严格相等比较仍可能为 false，因为它们不是同一引用。",
                    "map 生成等长的新数组，filter 按条件保留元素，reduce 汇总结果；选方法时先说清输入与输出形状。"),
            e("条件、循环与函数",
                    "空字符串、0、NaN、null、undefined 和 false 会转为假，其余多数值为真；业务判断应避免把合法的 0 误当成缺失。",
                    "函数的 return 会立即结束本次调用并把值交回调用处，console.log 只产生观察输出，不能代替返回值参与后续计算。",
                    "闭包让函数在外层调用结束后仍能访问其词法作用域变量，适合封装状态，也可能因长期引用导致资源无法释放。"),
            e("找到并修改页面元素",
                    "querySelector 只返回首个匹配项，querySelectorAll 返回静态 NodeList；批量处理前应核对数量而不是假设元素一定存在。",
                    "textContent 把输入作为文字处理，innerHTML 会把字符串解析成节点；未经可信清洗的用户内容进入 innerHTML 会带来脚本注入风险。",
                    "连续读取布局尺寸再修改样式可能反复触发布局计算，应批量更新类名，并在动画场景减少读写布局属性交错。"),
            e("事件与页面状态",
                    "事件通常经历捕获、目标和冒泡阶段；把监听器放在共同父元素上可利用冒泡处理动态列表，这种方式称为事件委托。",
                    "preventDefault 阻止浏览器默认行为，stopPropagation 阻止事件继续传播，二者解决的问题不同，不能互相替代。",
                    "界面状态应明确有限取值与转移条件，例如 idle、loading、success、error；按钮文字和禁用状态都从同一状态推导。"),
            e("异步请求与 Fetch",
                    "Fetch 只在网络层失败等情况下拒绝 Promise，HTTP 404 或 500 仍会得到 Response，因此必须检查 response.ok 或状态码。",
                    "较晚发出的请求可能先返回，搜索联想等场景要用 AbortController 或请求编号丢弃过期响应，避免旧结果覆盖新输入。",
                    "响应正文通常只能消费一次，调用 json 后不能再调用 text 读取同一数据流；解析前还应根据 Content-Type 核对返回格式。"),

            e("为什么需要 Vue",
                    "Vue 在渲染期间记录响应式状态的读取关系，状态变化后只让依赖它的渲染副作用重新执行，从而保持数据与界面同步。",
                    "虚拟 DOM 是描述界面的 JavaScript 结构，更新时通过差异比较减少真实 DOM 操作，但性能仍取决于组件边界与稳定 key。",
                    "框架解决状态组织和组件协作，HTML 语义、CSS 布局、JavaScript 类型与浏览器 API 仍是排查问题的基础。"),
            e("创建 Vue 3 项目",
                    "Vite 开发时按需转换模块并提供热更新，生产构建会打包、压缩并生成带哈希资源，两种运行方式必须分别验证。",
                    "Vue 单文件组件会经过编译器处理 template 与 scoped style，浏览器最终执行的是转换后的 JavaScript 和普通 CSS。",
                    "锁文件固定直接与传递依赖的解析版本，从空环境安装并构建一次，才能证明项目不是只在当前电脑偶然可运行。"),
            e("响应式数据与模板",
                    "ref 通过 value 保存响应式值，模板会自动解包顶层 ref；reactive 适合对象代理，但解构普通属性可能丢失响应式连接。",
                    "computed 缓存由依赖推导的结果，依赖未变时不会重复计算；watch 用于响应变化执行异步请求或外部副作用。",
                    "模板插值会转义文本，动态属性使用 v-bind，事件使用 v-on；把带副作用的复杂表达式塞进模板会降低可预测性。"),
            e("组件、Props 与事件",
                    "Props 是父组件传入的只读输入，子组件通过 emit 报告事件，由拥有状态的一方决定如何修改，形成单向数据流。",
                    "插槽让父组件提供一段结构内容，Props 适合传数据；二者选择取决于变化的是数据还是整块界面结构。",
                    "组件暴露的 Props、事件和插槽共同构成公开契约，重构内部实现时应保持契约稳定或明确更新所有调用方。"),
            e("列表、表单与条件渲染",
                    "列表 key 表示项目身份，使用数组下标会在插入、删除和排序后错误复用组件状态，应优先使用稳定业务编号。",
                    "v-if 会创建与销毁内容，v-show 只切换 display；频繁切换适合 v-show，初始化昂贵但很少显示的内容适合 v-if。",
                    "v-model 是属性与更新事件的语法组合，修饰符会改变同步时机或类型；提交前仍要处理空白、范围和服务端错误。"),
            e("路由、状态与接口",
                    "同一路由组件的参数变化可能复用组件实例，依赖路由参数的数据应监听参数或使用路由守卫重新加载。",
                    "Pinia 保存跨页面共享且具有业务意义的状态，临时展开开关等局部状态留在组件中能减少全局耦合。",
                    "接口层应统一令牌、错误结构与序列化，页面维护 loading、empty、error、success，避免每个组件重复写不一致逻辑。"),
            e("Vue 个人网站实战",
                    "先确定从首页进入作品详情的最小可用路径，再按路由、页面、组件和数据逐层实现，可以更早得到可运行版本。",
                    "语义结构、键盘焦点、响应式布局和加载失败都属于功能完成条件，应写进验收清单而不是最后临时补救。",
                    "交付前从空依赖目录安装、执行类型检查和生产构建，再用构建产物检查直接访问与刷新子路由是否正常。"),

            e("认识 Python Web 接口",
                    "FastAPI 运行在 ASGI 协议上，async def 适合等待异步 I/O；普通阻塞库放进 async 函数会阻塞事件循环。",
                    "类型标注同时参与请求解析、数据校验和 OpenAPI 文档生成，但函数内部的业务正确性仍由代码与测试保证。",
                    "开发服务器提供自动重载便于本地调试，生产环境还要处理进程管理、超时、日志、健康检查与安全配置。"),
            e("路由与请求参数",
                    "固定路径与动态路径可能发生匹配竞争，例如 /posts/latest 应在 /posts/{id} 之前声明，避免 latest 被当作编号解析。",
                    "路径参数标识资源，查询参数表达筛选或分页，请求体承载结构化提交；把含义放错位置会让接口契约难以理解。",
                    "创建、读取、替换、部分更新与删除应选择匹配的 HTTP 方法和状态码，并明确重复请求是否产生相同结果。"),
            e("Pydantic 数据校验",
                    "输入模型负责接收允许字段，输出模型负责限制公开字段；分开定义可以避免密码哈希或内部状态被意外序列化。",
                    "字段约束处理长度、范围与格式，跨字段关系可用模型校验器；用户名唯一等依赖数据库的规则仍属于业务校验。",
                    "Pydantic 可能按规则转换输入类型，严格模式会减少隐式转换；选择前要明确接口是否接受字符串形式的数字。"),
            e("CRUD 与分层结构",
                    "路由层处理 HTTP 契约，服务层组织业务规则与事务，数据访问层封装查询；依赖方向应让业务逻辑可独立测试。",
                    "PUT 通常表达完整替换，PATCH 表达部分修改；更新时要区分未提供字段与明确提交 null，避免意外覆盖。",
                    "删除资源前要检查所有权与关联关系，并根据业务选择拒绝、级联或软删除，不能只调用一条 delete 后结束。"),
            e("SQLAlchemy 数据持久化",
                    "Session 表示一次工作单元，flush 把变更发送给数据库但不等于提交；异常后需要 rollback 才能继续安全使用会话。",
                    "关联对象的延迟加载可能在列表序列化时产生 N+1 查询，应通过 SQL 日志、预加载策略和查询次数验证。",
                    "表结构变化应由迁移工具生成可审查版本，运行时自动建表不能可靠处理生产环境中的改列、回填与回滚。"),
            e("跨域、测试与部署",
                    "CORS 由浏览器执行，带凭证请求不能随意使用通配来源；应列出可信前端地址、方法和首部并测试预检请求。",
                    "接口测试需要隔离数据库状态，覆盖成功、校验失败、未授权和依赖异常，并断言状态码与公开响应契约。",
                    "部署配置通过环境变量注入，启动后用健康检查和核心写读冒烟验证；回退前还要确认数据库迁移与旧版本兼容。")
    );

    public static List<String> pointsFor(String chapterTitle) {
        List<String> points = POINTS.get(chapterTitle);
        if (points == null) throw new IllegalArgumentException("缺少建站章节深度知识点：" + chapterTitle);
        return points;
    }

    private static Map.Entry<String, List<String>> e(String title, String first, String second, String third) {
        return Map.entry(title, List.of(first, second, third));
    }
}
