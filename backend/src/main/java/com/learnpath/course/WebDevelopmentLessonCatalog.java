package com.learnpath.course;

import java.util.Map;

/** Beginner-first explanations for the website-building course family. */
public final class WebDevelopmentLessonCatalog {

    private WebDevelopmentLessonCatalog() {
    }

    private static final Map<String, Lesson> LESSONS = Map.ofEntries(
            e("网页从哪里开始", "先弄清浏览器、网页文件和网址分别做什么，并亲手打开第一个页面。", "浏览器像读信的人，HTML 文件像信纸，网址像信封上的地址。", "新建 index.html，写下标题和一段自我介绍，再用浏览器打开它。", "浏览器读取 HTML、CSS 和 JavaScript 后把代码渲染成可见页面。", "开发者工具可以检查元素、样式、网络请求和控制台错误。", "index.html 通常作为网站入口，文件之间使用相对路径互相引用。", "建立 first-site 文件夹，完成可双击打开的自我介绍页，并用开发者工具改一次标题。"),
            e("用 HTML 组织内容", "学习用标题、段落、图片、链接和列表表达内容结构。", "HTML 像给文章贴标签：这是标题、这是正文、这是图片，不负责把它们涂成什么颜色。", "用 h1 写名字、p 写介绍、ul 列出兴趣，再加一张有替代文字的图片。", "元素通常由开始标签、内容和结束标签组成，属性补充地址、说明等信息。", "标题层级应按内容关系使用，不能只因字号大小随意跳级。", "图片需要有意义的 alt，链接文字应说明打开后会去哪里。", "把个人主页写成页头、个人介绍、兴趣列表和联系方式四个清楚区域。"),
            e("表单与语义标签", "让用户能够输入内容，同时让页面结构更容易被浏览器和辅助工具理解。", "表单像填写入社申请，label 是题目，input 是填写框，button 是交卷按钮。", "制作姓名和留言表单，让每个输入框都通过 label 得到清楚名称。", "header、nav、main、section 和 footer 描述页面区域的用途。", "表单控件需要名称、标签和合适的 type，前端校验只改善体验。", "button 默认行为受所在表单影响，应明确提交还是普通按钮。", "给个人网站加入联系表单，并仅用键盘完成一次填写与提交。"),
            e("CSS 选择器与层叠", "学习准确选中页面元素，并理解多条样式冲突时谁会生效。", "选择器像点名，属性和值像给被点到的人安排新衣服和座位。", "分别用元素、类和状态选择器改变标题、卡片及按钮悬停样式。", "CSS 规则由选择器和声明块组成，声明包含属性、冒号、值和分号。", "层叠会比较来源、重要性、选择器优先级和出现顺序。", "类选择器适合复用样式，避免用过长选择器把组件绑死在页面结构上。", "为主页建立颜色和文字样式，使用开发者工具解释一条被覆盖的规则。"),
            e("盒模型与 Flex 布局", "理解每个元素占多大空间，并让多个元素有规则地排成一行或一列。", "盒模型像快递盒：内容在中心，padding 是缓冲层，border 是纸箱，margin 是箱子之间的距离。", "给三张兴趣卡片设置 padding 和 gap，再用 Flex 让它们自动排队。", "标准盒模型由 content、padding、border 和 margin 组成。", "box-sizing: border-box 让声明宽度包含 padding 与 border，更容易计算。", "Flex 的主轴由 flex-direction 决定，justify-content 和 align-items 分别控制两条轴。", "制作三张等宽兴趣卡片，在宽屏横排、窄屏自动换行。"),
            e("响应式个人主页", "让同一个网站在手机、平板和电脑上都能自然阅读和操作。", "响应式设计像会伸缩的书架，空间变小会重新排列，而不是把所有东西硬塞进去。", "缩窄浏览器，使用媒体查询把双栏介绍改成单栏，并扩大按钮触控区域。", "viewport 元信息让移动浏览器按设备宽度计算布局。", "弹性宽度、max-width 和媒体查询共同处理不同屏幕。", "响应式断点应由内容何时拥挤决定，不应只按某个手机型号决定。", "完成个人主页的手机布局，检查 320px 宽度下没有横向滚动且按钮易于点击。"),

            e("JavaScript 第一次登场", "看懂 JavaScript 在网页中负责什么，并让一句代码真正改变页面。", "HTML 搭舞台，CSS 管布景，JavaScript 像演员和机关，让按钮按下后发生事情。", "在控制台输出问候，再让脚本把页面标题改成自己的名字。", "JavaScript 由浏览器执行，可以读取页面、响应操作和发起网络请求。", "script 使用 defer 可等待 HTML 解析，同时避免阻塞页面显示。", "控制台报错会指出文件和行号，应该从第一条错误开始排查。", "给个人主页连接 app.js，让页面加载后显示一条欢迎消息。"),
            e("变量、类型与数组", "学会给数据起名字，并用数组保存一组作品、兴趣或文章。", "变量像贴标签的收纳盒，数组像按顺序编号的一排抽屉。", "把姓名保存到字符串，把是否夜间模式保存为布尔值，把三个兴趣保存到数组。", "const 表示绑定不重新赋值，let 用于确实需要变化的状态。", "常见类型包含 string、number、boolean、null、undefined、object。", "数组下标从 0 开始，map、filter 等方法适合把数据转换成界面内容。", "创建作品数组，逐项生成标题与简介并在控制台输出。"),
            e("条件、循环与函数", "让程序根据情况选择动作，并把重复工作整理成可以再次调用的函数。", "if 像岔路口，循环像逐个点名，函数像可以反复使用的饮料机。", "写 greet(name)，有名字就问候，没有名字就显示“欢迎来访”。", "条件表达式最终按真假决定分支，=== 会同时比较类型和值。", "函数通过参数接收输入，通过 return 给出结果并形成清楚边界。", "遍历页面数据时优先选择表达意图清楚的数组方法。", "用函数生成兴趣卡片，并为空数组显示友好的空状态。"),
            e("找到并修改页面元素", "用 DOM 找到 HTML 元素，再安全地修改文字、类名和属性。", "DOM 像浏览器整理出的页面家谱，JavaScript 可以按名字找到成员并更新。", "用 querySelector 找到标题，修改 textContent，再给卡片添加 highlight 类。", "document.querySelector 使用 CSS 选择器并只返回第一个匹配元素。", "textContent 适合普通文字，未经处理的用户内容不应直接交给 innerHTML。", "classList.add、remove 和 toggle 用类名控制视觉状态。", "为主页做一个编辑按钮，点击后更新介绍文字并给卡片加高亮。"),
            e("事件与页面状态", "理解点击、输入和提交事件，并让页面记住当前处于什么状态。", "事件像门铃，监听器是听见门铃后去开门的人，状态记录门现在开着还是关着。", "点击主题按钮切换 dark 类，同时更新按钮文字为“白天”或“夜间”。", "addEventListener 把事件类型和处理函数连接起来。", "状态是影响界面的数据，改变状态后要同步更新显示。", "表单 submit 应阻止不需要的默认刷新，并处理重复提交。", "完成主题切换和作品筛选，说明每个交互改变了哪个状态。"),
            e("异步请求与 Fetch", "让网页向后端请求数据，并处理等待、成功和失败三种结果。", "点外卖后不会站在店里等，先拿到订单号做别的事；数据送到时再处理，这就是异步。", "使用 fetch 请求作品列表，加载时显示提示，成功后渲染，失败后提供重试按钮。", "Fetch 返回 Promise，await 会在异步函数中等待结果而不冻结整个页面。", "HTTP 非 2xx 不会自动让 Fetch 抛错，需要检查 response.ok。", "请求界面至少要考虑 loading、empty、error 和 success 状态。", "从一个 JSON 接口读取文章卡片，断网后仍能看到明确错误和重新加载按钮。"),

            e("为什么需要 Vue", "理解 Vue 解决的实际问题，以及它和 HTML、CSS、JavaScript 的关系。", "普通页面像手工管理很多便利贴，Vue 像一个会自动同步内容的看板系统。", "把计数变量交给 Vue，在按钮点击后修改它，页面数字自动更新。", "Vue 是前端框架，不替代 HTML、CSS 和 JavaScript，而是组织它们。", "声明式渲染描述状态对应什么界面，Vue 负责高效更新 DOM。", "组件把一块界面及其逻辑封装起来，便于复用和维护。", "对比手动修改 DOM 与 Vue 响应式写法，完成一个点赞按钮。"),
            e("创建 Vue 3 项目", "用 Vite 创建并运行项目，认清入口、组件和开发服务器。", "脚手架像准备好的工作台，常用工具已经摆好，但作品内容仍由你完成。", "创建 Vue 项目，启动开发服务器，修改 App.vue 后观察页面自动更新。", "package.json 记录脚本和依赖，src 保存主要源代码。", "单文件组件把 template、script 和 style 放在一个有明确职责的文件中。", "开发服务器热更新用于开发，最终上线前仍需执行生产构建。", "创建个人网站项目，清理示例内容并保留一个能正常渲染的首页组件。"),
            e("响应式数据与模板", "让变量变化后自动更新页面，并用模板表达文字、属性和事件。", "响应式数据像连接显示屏的温度计，数字一变，显示屏不用人工重写。", "用 ref 保存昵称和点赞数，在模板中展示并通过按钮修改。", "ref 适合单个响应式值，在脚本中通过 .value 访问。", "文本插值会安全输出普通文字，v-bind 绑定属性，v-on 监听事件。", "computed 从其他状态推导结果，应避免在其中执行修改状态的副作用。", "完成可编辑个人签名，输入内容时预览标题实时变化。"),
            e("组件、Props 与事件", "把页面拆成清楚的小组件，并让父子组件正确交换信息。", "组件像乐高积木，Props 是父组件交给积木的材料，事件是积木向父组件发出的消息。", "建立 ProjectCard，通过 Props 接收标题，点击时 emit 选择事件。", "Props 是从父到子的只读数据流，子组件不应直接修改它。", "emit 用命名事件把用户动作通知父组件，由父组件决定如何更新状态。", "组件边界应围绕独立职责和复用价值，而不是把每个 div 都拆开。", "将三个作品卡片改成组件，并在父组件显示当前选中的作品标题。"),
            e("列表、表单与条件渲染", "把数组变成卡片列表，处理输入表单，并根据状态显示不同内容。", "数据像演员名单，v-for 让同一种卡片为每名演员各出现一次。", "用 v-for 渲染作品，用 v-model 编辑标题，用 v-if 显示空状态。", "v-for 需要稳定且唯一的 key，避免更新时复用错误节点。", "v-model 连接输入值与状态，提交时仍要进行数据校验。", "v-if 适合条件切换，v-show 适合频繁显示隐藏但内容始终存在的区域。", "完成作品新增表单，可添加卡片，并在列表为空时展示引导文字。"),
            e("路由、状态与接口", "让单页应用拥有多个地址、共享状态，并从后端读取真实数据。", "路由像商场楼层索引，Store 像公共寄存柜，接口像连接后厨的取餐口。", "添加首页与作品详情路由，用 Pinia 保存用户偏好，再通过 Fetch 加载作品。", "Vue Router 把 URL 映射到组件，并支持参数与导航守卫。", "Pinia 适合跨页面或跨组件共享状态，本地状态仍应留在组件中。", "接口调用要集中处理鉴权、错误和响应结构，页面负责呈现状态。", "完成作品列表到详情页的跳转，刷新详情页仍能按 URL 读取正确数据。"),
            e("Vue 个人网站实战", "把前面知识组合成一个能运行、能构建、能展示的完整前端作品。", "实战像组装整台电脑：零件都学过，这次重点是让它们稳定协作。", "规划首页、作品页和关于页，拆组件、接数据、处理空状态并完成构建。", "项目先确定最小可用流程，再按页面和组件逐步实现。", "可访问性、响应式、加载错误和键盘操作都属于完成标准。", "交付前应从空环境安装依赖并执行构建，确保项目可复现。", "完成个人网站并提交需求清单、页面地址、运行说明和一次自测记录。"),

            e("认识 Python Web 接口", "了解后端在网站中的位置，并用 FastAPI 返回第一份 JSON。", "前端像餐厅大堂，后端像厨房；FastAPI 是把点单规则变成清楚接口的工具。", "创建 /hello 路由，启动服务后在浏览器看到 message 为“你好”的 JSON。", "后端接收请求、执行规则、访问数据并返回响应。", "FastAPI 根据 Python 类型标注生成校验与交互式接口文档。", "开发服务器只用于本地调试，公开部署需要正式运行方式和配置。", "创建 FastAPI 项目，完成健康检查和问候接口并在 /docs 中试调。"),
            e("路由与请求参数", "用路径、查询参数和请求体表达客户端想查询或提交的数据。", "网址路径像楼层和房间号，查询参数像附加要求，请求体像完整申请表。", "建立 GET /posts/{id}、GET /posts?keyword= 和 POST /posts 三个接口。", "路径参数标识具体资源，查询参数常用于筛选、分页和排序。", "GET 用于读取，POST 常用于创建；状态码应准确说明结果。", "接口名称与字段要围绕资源保持一致，避免把动作随意写进每个 URL。", "为个人博客设计文章列表、详情与创建接口，并列出请求和响应示例。"),
            e("Pydantic 数据校验", "在数据进入业务逻辑前检查类型、长度和必填规则，并把错误说清楚。", "Pydantic 像入口检票员，先检查票面信息，再允许数据进入系统。", "为文章标题设置 1 到 80 个字符，为内容设置最小长度，提交错误数据观察 422。", "BaseModel 描述请求和响应结构，Field 可以声明长度和范围。", "类型校验不能替代业务规则，例如文章作者权限仍需在服务层检查。", "单独定义输入和输出模型可以避免泄露内部字段。", "为注册和文章创建接口设计模型，验证空标题、超长标题和非法邮箱。"),
            e("CRUD 与分层结构", "完成新增、查询、修改和删除，并把路由、业务和数据访问分开。", "路由像前台接待，Service 像负责人，Repository 像档案管理员。", "为文章实现 CRUD，找不到文章时统一返回 404。", "CRUD 分别对应 Create、Read、Update、Delete 四类基本操作。", "分层让 HTTP 细节、业务规则和存储实现各自保持清楚。", "更新接口要明确完整替换还是部分修改，删除要考虑权限和关联数据。", "完成文章 CRUD，并为重复标题和不存在编号写出可读错误响应。"),
            e("SQLAlchemy 数据持久化", "把内存中的文章保存进数据库，并使用事务保证一组修改共同成功。", "ORM 像翻译员，在 Python 对象和数据库表之间转换，但底层仍然执行 SQL。", "定义 Article 模型，保存文章后重启服务，再次查询仍能得到数据。", "Session 表示一段数据库工作范围，需要正确提交、回滚和关闭。", "表关系、唯一约束和索引应由真实数据规则决定。", "ORM 方便开发但不能忽略生成的 SQL、查询次数和事务边界。", "连接 SQLite 或 MySQL，完成文章持久化，并验证非法外键与重复值被拒绝。"),
            e("跨域、测试与部署", "让前端安全访问接口，用自动测试守住行为，并把服务部署到可运行环境。", "跨域像不同校区之间的门禁，CORS 明确哪些来源能进；测试像每次开门前自动检查门锁。", "允许本地 Vue 地址访问接口，编写一个成功和一个失败测试，再用环境变量启动服务。", "CORS 应列出可信来源，不应在带凭证场景随意允许全部来源。", "TestClient 可以验证状态码与 JSON，测试数据应彼此隔离。", "部署时需外置数据库地址与密钥，并提供健康检查和日志。", "完成 Vue 与 FastAPI 联调，运行测试后部署，并记录启动、验证和回退步骤。")
    );

    public static Lesson lessonFor(String title) {
        return LESSONS.get(title);
    }

    private static Map.Entry<String, Lesson> e(String title, String intro, String analogy, String example,
                                                String foundation, String method, String application, String practice) {
        return Map.entry(title, new Lesson(intro, analogy, example, foundation, method, application, practice));
    }

    public record Lesson(String intro, String analogy, String example, String foundation,
                         String method, String application, String practice) {
    }
}
