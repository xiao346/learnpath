package com.learnpath.course;

import java.util.List;
import java.util.Map;

/** Curated Chinese lesson notes used to populate every chapter in the learning database. */
public final class ChapterContentCatalog {

    private ChapterContentCatalog() {
    }

    private static final Map<String, Content> CONTENT = Map.ofEntries(
            entry("算法与复杂度入门", "算法是输入到输出的有限步骤；复杂度用于比较数据规模增长时资源消耗的变化。", "时间复杂度忽略常数和低阶项，常见数量级依次为 O(1)、O(log n)、O(n)、O(n log n)、O(n²)。", "分析循环要看执行次数，分析递归要写递推关系；空间复杂度还要计入递归栈和辅助容器。", "分别分析顺序查找和二分查找的最好、最坏时间复杂度，并用 1 万个有序整数实测比较次数。"),
            entry("线性表与顺序存储", "线性表强调元素之间的一对一前驱后继关系，数组用连续地址实现随机访问。", "顺序表按下标访问是 O(1)，但中间插入和删除需要搬移元素，最坏为 O(n)。", "动态数组容量不足时通常倍增扩容，以一次昂贵复制换取追加操作的均摊 O(1)。", "实现支持增删改查和自动扩容的动态数组，编写边界测试覆盖空表、首尾插入和越界访问。"),
            entry("栈、队列与应用", "栈遵循后进先出，队列遵循先进先出，两者都限制了元素的访问位置。", "栈适合括号匹配、表达式求值和函数调用；队列适合任务调度、消息缓冲和广度优先搜索。", "数组实现循环队列时用取模让头尾指针回绕，并额外记录长度或预留空位区分队空与队满。", "实现括号匹配器和循环队列，用至少 6 组正常与异常输入验证入栈、出栈、入队和出队。"),
            entry("二叉树与遍历算法", "二叉树每个节点最多有两个孩子，层次结构适合表达分类、搜索和优先关系。", "前序为根左右，中序为左根右，后序为左右根；递归写法的差异只在访问根节点的位置。", "层序遍历借助队列逐层访问；n 个节点的遍历时间为 O(n)，递归额外空间取决于树高。", "手绘一棵 9 节点二叉树，写出四种遍历序列，再分别用递归和迭代代码验证结果。"),
            entry("图的表示与遍历", "图由顶点和边组成，可表示社交关系、道路、依赖等多对多结构。", "邻接矩阵查询两点是否相邻为 O(1) 但占 O(V²)；邻接表更适合稀疏图，占 O(V+E)。", "DFS 使用递归或栈沿路径深入，BFS 使用队列逐层扩展；两者都必须用 visited 防止重复访问。", "用邻接表实现无向图的 DFS 与 BFS，输出访问顺序并判断两个指定顶点是否连通。"),
            entry("查找算法", "查找的核心是在给定约束下缩小候选范围，数据是否有序决定可用策略。", "顺序查找无需预处理但为 O(n)；二分查找要求有序且每次排除一半，复杂度为 O(log n)。", "哈希查找平均接近 O(1)，但需要设计哈希函数并用拉链法或开放寻址处理冲突。", "实现左边界二分查找，处理重复元素和空数组；再构造哈希冲突案例比较两种冲突处理方法。"),
            entry("排序算法", "排序算法要同时比较时间、空间、稳定性和数据特征，不能只看平均复杂度。", "插入排序对近乎有序数据有效；归并排序稳定且为 O(n log n)，但需要 O(n) 辅助空间。", "快速排序平均 O(n log n)，基准选择不佳时退化为 O(n²)，随机基准可降低退化概率。", "对随机、逆序、近乎有序三组数据测试插入、归并、快速排序，记录耗时并解释差异。"),
            entry("综合实践与复盘", "综合题的关键是先识别数据关系和主要操作，再选择结构并证明复杂度。", "需求中的“频繁查询、保持顺序、动态增删”等词分别暗示哈希、树、链表或堆等候选结构。", "复盘应包含正确性、复杂度、边界测试和可维护性，不能只保留一份能够运行的代码。", "完成一个课程任务调度器：按优先级取任务、按编号查询任务，并提交设计说明、测试结果和复杂度分析。"),

            entry("数据库系统概述", "数据库管理系统统一负责数据定义、存取、并发、安全和恢复，应用不直接管理底层文件。", "三级模式由外模式、模式和内模式组成，两级映像提供逻辑与物理数据独立性。", "关系数据库以表表达实体和联系，主键唯一标识元组，外键维护表之间的引用关系。", "为教务场景列出学生、课程、选课三个实体，画出关系并标注主键、外键和基数。"),
            entry("关系模型与关系代数", "关系模型把数据组织为属性、元组和关系，关系代数提供查询的形式化基础。", "选择筛选行，投影筛选列；并、差要求关系相容，笛卡尔积构造所有元组组合。", "连接把相关元组按条件组合，自然连接会自动匹配同名属性，使用时需警惕误匹配。", "用选择、投影和连接写出“选修数据库且成绩不低于 80 分的学生姓名”的关系代数表达式。"),
            entry("SQL 数据查询", "SELECT 查询按 FROM、WHERE、GROUP BY、HAVING、SELECT、ORDER BY 的逻辑顺序处理。", "WHERE 在分组前过滤行，HAVING 在聚合后过滤组；NULL 要用 IS NULL 判断。", "内连接只保留匹配行，左连接保留左表全部行；子查询与连接应依据可读性和执行计划选择。", "在 MySQL 创建选课数据，写出连接、分组聚合、相关子查询和窗口函数各一条并核对结果。"),
            entry("数据库完整性", "完整性约束在数据库层阻止无效数据进入，避免只依赖某一个应用的校验。", "实体完整性要求主键非空且唯一，参照完整性要求外键引用存在的候选键或为空。", "NOT NULL、UNIQUE、CHECK、DEFAULT 处理列规则；事务和触发器可处理跨行或跨表规则。", "为订单表设计金额、状态、客户引用和订单号约束，尝试插入 5 类非法数据并记录数据库错误。"),
            entry("关系数据库设计", "规范化通过拆分不合理关系减少冗余以及插入、删除和更新异常。", "函数依赖 X→Y 表示 X 值确定 Y；候选键是能唯一决定全部属性的最小属性集。", "2NF 消除对候选键的部分依赖，3NF 消除非主属性对候选键的传递依赖，拆分还要保证无损连接。", "给定订单明细宽表，找出函数依赖与候选键，将其分解到 3NF 并说明每次分解消除的异常。"),
            entry("事务与并发控制", "事务用 ACID 保证一组操作的原子性、一致性、隔离性和持久性。", "脏读、不可重复读和幻读来自并发可见性差异，隔离级别越高通常并发代价越大。", "锁控制并发访问，MVCC 保存数据版本提升读写并发；死锁需要检测回滚并统一加锁顺序预防。", "在 DataGrip 打开两个会话，复现不可重复读，再切换隔离级别观察结果并查询锁等待信息。"),
            entry("索引优化与课程实践", "索引是以额外存储和写入成本换取查询速度的数据结构，必须围绕真实查询设计。", "B+ 树叶子节点有序且适合范围查询；联合索引遵循最左前缀，列顺序要考虑等值、范围和排序。", "EXPLAIN 可查看访问类型、候选索引、实际索引和估算行数，优化后应以真实数据量复测。", "为百万级订单表设计两个查询场景，比较建索引前后 EXPLAIN 与耗时，并解释未命中索引的原因。"),

            entry("现代 Web 开发概览", "Web 应用通过 HTTP 连接浏览器、服务端和数据存储，接口契约是前后端协作边界。", "典型分层包含控制器、业务服务、数据访问和领域模型，每层只承担清晰职责。", "JSON API 需要统一状态码、错误结构、分页和版本策略，可观测性则覆盖日志、指标与追踪。", "画出一次课程查询从浏览器到 MySQL 的调用链，标注每层输入输出以及可能失败的位置。"),
            entry("Spring Boot 快速入门", "Spring Boot 通过自动配置和 Starter 依赖减少样板配置，让应用能快速启动。", "@SpringBootApplication 组合配置、自动配置和组件扫描；依赖注入让对象协作可测试、可替换。", "配置应按环境外置，application.yml 提供默认值，环境变量覆盖敏感或部署相关参数。", "创建包含 health 接口的 Spring Boot 项目，分别用开发和测试配置启动，并解释自动配置报告。"),
            entry("RESTful API 设计", "REST API 围绕资源建模，用 URI 表示对象，用 HTTP 方法表达操作语义。", "GET 安全且幂等，PUT 和 DELETE 幂等，POST 通常不幂等；创建资源常返回 201 和 Location。", "2xx、4xx、5xx 分别表示成功、客户端问题和服务端问题，错误响应应含稳定错误码与可读消息。", "为课程、章节、学习进度设计一组 URI、方法、请求体和状态码，并用 curl 完成一次完整调用。"),
            entry("数据持久化", "JPA 用实体映射关系表，Repository 封装常见 CRUD，但开发者仍需理解实际 SQL。", "@Entity、@Id 和关联注解描述映射；LAZY 延迟加载可减少无关查询，但要在事务边界内访问。", "N+1 查询可用 EntityGraph、fetch join 或批量加载解决，任何优化都应通过 SQL 日志验证。", "建立课程与章节一对多映射，完成新增和查询，并确认列表接口没有出现 N+1 SQL。"),
            entry("参数校验与异常处理", "输入校验应在系统边界尽早拒绝无效数据，异常处理则负责输出一致的错误契约。", "Bean Validation 用 @NotBlank、@Min 等声明规则，嵌套对象需配合 @Valid 才会继续校验。", "@RestControllerAdvice 可集中转换业务异常、校验异常和未知异常，同时避免把堆栈暴露给客户端。", "为注册接口补齐 6 条校验规则，编写异常处理器，并用测试验证状态码、字段错误和消息。"),
            entry("身份认证与权限控制", "认证回答“你是谁”，授权回答“你能做什么”，两者必须在服务端强制执行。", "会话令牌应随机、限时并支持失效；密码只存带盐哈希，日志和响应不得泄露凭证。", "基于角色的权限控制适合粗粒度规则，资源所有权检查用于限制用户只能访问自己的数据。", "设计学生、教师、管理员权限矩阵，为两个受保护接口编写未登录、越权和正常访问测试。"),
            entry("Redis 缓存实践", "缓存把高频数据放到更快的存储中，但必须处理过期、一致性和击穿问题。", "Cache Aside 先读缓存，未命中再读数据库并回填；更新时通常先写数据库再删除缓存。", "TTL 防止永久陈旧，随机抖动缓解雪崩；互斥锁或逻辑过期可处理热点键击穿。", "为课程详情添加 Redis 缓存，记录命中率，并验证更新课程后缓存能够正确失效。"),
            entry("接口测试", "接口测试验证从 HTTP 输入到响应契约的完整行为，单元测试则聚焦单个类的逻辑。", "正常、边界、无效输入、未认证和越权是最基本用例；测试数据必须可重复且彼此隔离。", "MockMvc 适合测试 Spring MVC 层，集成测试可配合 H2 或容器验证真实持久化行为。", "为课程详情和答题提交接口各写 5 个测试，覆盖成功、404、校验失败和权限错误。"),
            entry("部署与综合实战", "交付一个 Web 服务不仅要能启动，还要具备配置、迁移、健康检查和故障定位能力。", "构建产物应可重复；密钥用环境变量或密钥服务注入，不能写进镜像与 Git。", "数据库变更应使用版本化迁移；部署后用健康检查、结构化日志和指标验证服务状态。", "将课程平台打包并连接 MySQL、Redis 启动，完成健康检查、核心接口冒烟测试和回滚说明。"),

            entry("网络体系结构", "分层把复杂通信拆成可替换的职责，发送端逐层封装，接收端逐层解封装。", "OSI 有七层，TCP/IP 常按链路、网际、传输和应用层理解；每层通过协议与对等实体通信。", "交换机主要依据 MAC 帧转发，路由器依据 IP 包选择路径，端口号定位主机上的具体进程。", "跟踪浏览器访问网站时的 DNS、TCP、TLS、HTTP 流程，标注每一步所在层和数据单元。"),
            entry("物理层基础", "物理层负责在介质上传输比特，信号、带宽、噪声和编码决定传输能力。", "比特率表示每秒比特数，波特率表示每秒符号数，一个符号可承载多个比特。", "香农公式描述有噪声信道容量上限；双绞线、光纤和无线介质在距离、带宽、抗干扰上不同。", "为宿舍到机房的网络连接比较三种介质，计算给定文件在 100 Mbps 理想链路上的最短传输时间。"),
            entry("数据链路层", "数据链路层在相邻节点间传输帧，处理成帧、差错检测和介质访问。", "以太网帧包含源/目的 MAC 与类型字段，CRC 能检测传输错误但通常不负责纠正。", "交换机学习源 MAC 与端口映射，再按目的 MAC 转发、过滤或泛洪；VLAN 划分广播域。", "观察一次 ARP 后的以太网帧，解释交换机地址表如何变化，并计算最小帧相关字段长度。"),
            entry("网络层与 IP", "网络层负责跨网络寻址与转发，IP 提供尽力而为、无连接的数据报服务。", "IPv4 地址由网络前缀和主机部分组成，CIDR 用 /n 表示前缀长度并支持路由聚合。", "ARP 在局域网解析下一跳 MAC；ICMP 传递差错和诊断信息，ping 与 traceroute 都依赖它。", "将 192.168.10.0/24 划分为四个等长子网，写出网络地址、可用范围和广播地址。"),
            entry("路由选择", "路由表决定数据包下一跳，最长前缀匹配选择最具体的可达路由。", "距离向量协议交换到目的地的距离，链路状态协议传播拓扑并运行最短路径算法。", "RIP 以跳数度量，OSPF 适合自治系统内部，BGP 在自治系统之间基于策略选择路径。", "给定 6 个路由器的带权拓扑，手算 Dijkstra 最短路径，并模拟一条链路失效后的路由变化。"),
            entry("TCP 与 UDP", "传输层用端口提供进程到进程通信，TCP 面向连接可靠传输，UDP 保留轻量报文语义。", "TCP 三次握手同步序号，序号与确认号保证有序交付，重传和滑动窗口处理丢包与流量控制。", "拥塞控制通过慢启动、拥塞避免等调整发送窗口；实时音视频常用 UDP 并在应用层权衡时延和可靠性。", "用 Wireshark 捕获一次 TCP 连接，标出握手序号、数据确认和挥手过程，再与 DNS 的 UDP 报文比较。"),
            entry("应用层协议", "应用层协议规定应用交换消息的格式和语义，常见协议包括 DNS、HTTP、SMTP 和 DHCP。", "DNS 按层次命名并使用缓存；HTTP 请求由方法、目标、首部和可选消息体组成。", "HTTPS 是 HTTP 运行在 TLS 之上，通过证书验证身份并协商加密密钥，保护机密性和完整性。", "使用浏览器开发者工具分析一个 HTTPS 请求，记录 DNS、连接、请求、响应状态码和缓存首部。"),
            entry("网络安全基础", "网络安全关注机密性、完整性和可用性，风险来自窃听、篡改、伪装与拒绝服务。", "对称加密适合大量数据，非对称加密解决密钥分发与数字签名，哈希用于完整性校验。", "TLS 结合证书、密钥交换和对称加密；防火墙、最小权限和及时更新构成纵深防御。", "检查一个网站证书的域名、签发者和有效期，解释浏览器如何建立信任并列出三项安全加固措施。"),

            entry("Campus Life", "本节用校园场景训练信息获取与口语表达，重点是课程、社团和时间安排。", "一般现在时描述日常安排，频率副词通常放在实义动词前、be 动词后。", "听读时先抓人物、地点、时间和行动；表达建议可用 You could、Why not 和 If I were you。", "阅读一则中文讲解后的校园通知，整理 5W1H 信息，再录制 90 秒英文自我介绍与每周安排。"),
            entry("Technology and Society", "本节讨论技术如何改变学习、工作和隐私，训练观点与证据的对应关系。", "表达因果可用 because、therefore、lead to；让步转折可用 although、however、on the other hand。", "事实可以核验，观点需要论据支持；阅读时标注作者主张、证据和隐含假设。", "围绕“生成式 AI 是否应进入课堂”列出正反各两条证据，完成一段 150 词有让步结构的英文短文。"),
            entry("Academic Reading", "学术阅读不是逐词翻译，而是根据标题、摘要、段首句快速建立文章结构。", "略读用于把握主旨，寻读用于定位数据与术语；陌生词先根据词根、同位语和上下文推断。", "论证通常由 claim、evidence、reasoning 组成，引用与例证的作用是支持而不是替代作者观点。", "选择一篇 800 词英文科普文章，写出段落功能提纲、5 个关键词和 100 字中文摘要。"),
            entry("Presentation Skills", "学术展示要让听众清楚知道问题、方法、发现和结论，而不是把全文搬到幻灯片。", "开场说明主题与路线，主体用 signposting 衔接，结尾回扣核心信息并提出行动或问题。", "每页只承载一个要点，图表标题应表达结论；语速、停顿、眼神和时间控制共同影响理解。", "制作 5 页英文展示并完成 3 分钟录制，检查是否包含清晰开场、三处路标语和有结论的图表。"),
            entry("Critical Writing", "批判性写作要求对观点进行比较、评价和论证，而不只是罗列材料。", "段落可采用主题句、证据、分析、回扣的 PEEL 结构；证据后必须解释它为何支持论点。", "改写需同时改变词汇和句法并标注来源，引用格式一致可避免无意抄袭。", "针对同一议题阅读两篇立场不同的材料，写 250 词比较段落并用高亮标出 claim、evidence 和 analysis。"),
            entry("Final Project", "结课项目整合检索、阅读、写作和展示能力，用一个真实问题产出可交流的成果。", "先把宽泛主题缩成可回答的 research question，再确定受众、证据标准和交付形式。", "初稿后按内容、结构、语言三轮修订；同伴反馈要具体指出位置、影响和可执行建议。", "完成一篇 600 词英文报告和 5 分钟展示，附资料来源、修订记录以及对学习策略的中文反思。"),

            entry("人工智能的过去与现在", "人工智能研究让机器完成感知、推理、学习和决策任务，能力边界随数据与算力演进。", "符号主义依赖规则和知识表示，机器学习从数据学习模式，现代系统常结合多种方法。", "区分专用人工智能与通用智能；评价产品时要看任务、数据、指标和失败条件，而不是只看演示。", "选择一个常用 AI 产品，画出输入—模型—输出链路，并记录三个有效场景和三个失败案例。"),
            entry("问题求解与搜索", "搜索把问题表示为状态、动作、初始状态和目标测试，再系统探索解空间。", "BFS 在单位代价图中保证最短路径但占内存多，DFS 内存较少却可能走入很深的无效分支。", "A* 用 f(n)=g(n)+h(n) 排序节点；可采纳启发式不高估剩余代价时可保证最优性。", "为八数码问题定义状态和动作，实现 BFS 或 A*，比较扩展节点数并说明启发函数是否可采纳。"),
            entry("知识表示", "知识表示把实体、属性、关系和规则编码成机器可操作的形式，从而支持推理。", "命题逻辑表达真假命题，一阶逻辑加入对象、谓词和量词，表达能力更强但推理更复杂。", "知识图谱以三元组连接实体，规则系统用前提推出结论；开放世界与封闭世界假设会改变缺失信息含义。", "为课程平台设计 15 个知识图谱三元组和 3 条推荐规则，演示如何推导一条新结论。"),
            entry("机器学习基础", "机器学习从样本估计可泛化规律，训练集用于拟合，验证集调参，测试集只做最终评估。", "监督学习处理带标签预测，无监督学习发现结构；分类和回归分别预测离散类别与连续数值。", "过拟合表现为训练好、测试差，可通过更多数据、正则化、简化模型和交叉验证缓解。", "用公开表格数据训练一个分类器，报告训练/测试指标、混淆矩阵，并分析至少一种主要错误。"),
            entry("神经网络初步", "神经网络由线性变换与非线性激活叠加，通过损失函数衡量预测误差。", "前向传播计算预测，反向传播用链式法则求梯度，优化器沿降低损失的方向更新参数。", "学习率过大可能震荡，过小收敛慢；训练曲线应同时观察训练集和验证集以发现过拟合。", "用 TensorFlow 中文教程训练手写数字模型，绘制损失和准确率曲线并解释一个错误预测。"),
            entry("自然语言处理", "自然语言处理把文本转成可计算表示，完成分类、检索、生成和信息抽取等任务。", "分词把文本拆成 token，词袋忽略顺序，向量嵌入用稠密空间表达语义相似性。", "Transformer 用自注意力建模长距离关系；生成模型的流畅输出不等于事实正确，需要检索与核验。", "构建一个中文情感分类小实验，说明分词、特征、训练、评估步骤，并检查 10 条误判样本。"),
            entry("负责任的人工智能", "负责任 AI 要在全生命周期处理公平、隐私、安全、透明和问责，而不是上线后的附加项。", "数据偏差、采样偏差和标签偏差会影响不同群体；总体准确率可能掩盖群体间表现差异。", "高风险决策需提供人工复核、申诉和审计记录；最小化数据、访问控制和红队测试降低风险。", "为“AI 简历筛选”建立风险清单，按利益相关者分析伤害，并提出指标、人工复核和退出机制。"),

            entry("Python 与开发环境", "Python 项目需要可重复的解释器和依赖环境，避免不同项目的包版本互相影响。", "虚拟环境隔离第三方包，requirements 或项目配置记录依赖，入口脚本负责清晰启动。", "良好工程习惯包括有意义命名、格式化、异常信息和版本控制；交互实验应整理成可运行脚本。", "创建虚拟环境，安装一个数据包，导出依赖并编写读取 CSV 的脚本，在全新环境复现运行。"),
            entry("数据类型与控制流程", "Python 的数值、字符串、列表、元组、字典和集合适合不同的数据关系与操作。", "可变对象能原地修改，不可变对象会产生新值；is 判断身份，== 判断值是否相等。", "if 负责分支，for 遍历可迭代对象，while 适合条件循环；推导式应保持简洁可读。", "读取一组学生成绩，用字典统计等级人数、最高分和平均分，并处理空列表和非法输入。"),
            entry("函数与模块", "函数把输入、处理和输出封装成可复用单元，模块与包进一步组织代码边界。", "位置参数、关键字参数和默认参数提升调用灵活性；可变默认参数会跨调用共享，应使用 None。", "异常用于传播无法就地处理的问题，try 只包裹可能失败的最小范围，finally 负责资源清理。", "把成绩统计拆成三个函数和一个模块，添加类型标注、文档字符串，并用断言测试正常与异常输入。"),
            entry("NumPy 数组计算", "NumPy ndarray 用同质连续数据支持高效向量化计算，是科学计算的基础。", "shape 描述维度，dtype 描述元素类型；切片常返回视图，修改视图可能影响原数组。", "广播从末尾维度对齐，维度相等或其中一个为 1 才兼容；向量化通常比 Python 循环更快。", "生成 1000×4 成绩矩阵，用广播标准化每列，筛选异常值并比较循环与向量化耗时。"),
            entry("Pandas 数据处理", "DataFrame 用带标签的行列组织异质表格数据，适合清洗、连接和聚合。", "loc 按标签选择，iloc 按位置选择；缺失值要根据业务含义删除、填补或保留。", "groupby 遵循拆分—应用—合并，merge 依据键连接表；连接前要检查键是否唯一以防行数膨胀。", "清洗一份订单 CSV：修正类型、处理缺失与重复、连接客户表，输出按月和品类的汇总结果。"),
            entry("数据可视化", "可视化的目标是准确传达比较、趋势、分布或关系，图形类型应匹配问题。", "柱状图比较类别，折线图展示时间趋势，散点图观察变量关系，直方图展示数值分布。", "坐标轴截断、双轴和面积编码容易误导；标题应说明结论，颜色要有语义且照顾可访问性。", "为同一数据制作趋势、分布和关系三张图，添加单位、来源和结论标题，并说明为何选择该图形。"),
            entry("综合分析项目", "数据分析项目从问题定义开始，经历获取、清洗、探索、验证、表达和复现。", "先写指标口径和分析假设，再检查数据质量；相关关系不能直接解释为因果关系。", "Notebook 适合探索，最终交付应能从原始数据一键生成结果，并记录环境、步骤与限制。", "选择一份公开中文数据，完成可复现报告：至少 3 个问题、2 种图表、1 项统计检验和结论限制。"),

            entry("软件过程与需求", "软件过程把需求、设计、实现、验证和交付串成可追踪流程，迭代能尽早暴露风险。", "需求要描述用户目标和验收条件，而不是直接指定技术方案；非功能需求包括性能、安全和可用性。", "用户故事说明角色、目标和价值，验收标准应具体可测试，需求变更需记录影响和决策。", "为学习平台写 5 条用户故事，每条给出 Given-When-Then 验收标准并标注优先级与风险。"),
            entry("Git 核心模型", "Git 用提交快照构成有向历史，工作区、暂存区和仓库对应修改的三个阶段。", "git add 把指定内容写入暂存区，git commit 创建快照；HEAD 指向当前分支的最新提交。", "对象库包含 blob、tree、commit 和 tag；提交哈希由内容决定，因此能检测历史内容变化。", "创建实验仓库，分别修改两个文件只暂存其中一个，提交后用 status、diff 和 log 解释三个区域状态。"),
            entry("分支与合并", "分支是指向提交的轻量指针，让不同工作线独立演进，切换分支会更新工作区。", "快进合并只移动指针，三方合并创建合并提交；rebase 重放提交以获得线性历史。", "共享分支不应随意改写历史，功能分支保持小而专注能降低冲突与评审成本。", "创建两个功能分支分别修改不同文件，完成 merge 与 rebase，对比 git log --graph 输出。"),
            entry("代码评审与冲突处理", "代码评审在合并前检查正确性、设计、测试和可维护性，也是团队共享知识的过程。", "评审意见应指出位置、影响和建议；作者要解释权衡并用新提交回应，避免只讨论个人偏好。", "冲突标记包含当前与另一侧内容，解决后必须重新测试；理解双方意图比机械选择一侧更重要。", "人为制造同一行冲突，保留两侧合理语义完成解决，并写一份包含风险与测试证据的评审说明。"),
            entry("自动化测试与持续集成", "自动化测试提供快速反馈，持续集成在每次变更时统一执行构建、测试和质量检查。", "单元测试数量多且快，集成测试验证组件协作，端到端测试覆盖关键用户路径但维护成本高。", "CI 工作流应固定运行环境、缓存依赖并保留失败日志；不稳定测试会削弱团队对流水线的信任。", "配置 GitHub Actions 中文文档中的基础工作流，让后端测试和前端构建在 push 与 PR 时自动执行。"),
            entry("版本发布实践", "版本发布把经过验证的代码转换为可识别、可部署和可回退的产品版本。", "语义化版本用主、次、修订号表达不兼容变更、新功能和修复；标签固定指向发布提交。", "变更日志面向用户说明影响，发布前检查数据库兼容、配置和回滚路径，发布后执行冒烟验证。", "为当前项目拟定 v1.1.0 发布说明，创建标签，列出部署、验证和回滚步骤并演练一次失败回退。")
    );

    public static Content contentFor(String chapterTitle) {
        Content content = CONTENT.get(chapterTitle);
        if (content == null) {
            WebDevelopmentLessonCatalog.Lesson lesson = WebDevelopmentLessonCatalog.lessonFor(chapterTitle);
            if (lesson == null) throw new IllegalArgumentException("缺少章节内容：" + chapterTitle);
            String overview = "这一章先解决一个看得见的建站问题。你会从最小示例开始，观察页面变化，再把同样方法用到自己的作品里。";
            return new Content(
                    overview,
                    List.of("能用自己的话解释：" + lesson.foundation(),
                            "能跟着示例演示并说明：" + lesson.method(),
                            "能在个人网站中正确应用：" + lesson.application()),
                    List.of(lesson.foundation(), lesson.method(), lesson.application()),
                    lesson.practice());
        }
        return content;
    }

    public static String expandedKeyPointsText(String chapterTitle) {
        Content content = contentFor(chapterTitle);
        java.util.ArrayList<String> points = new java.util.ArrayList<>(content.keyPoints());
        points.addAll(ChapterKnowledgeExpansion.pointsFor(chapterTitle));
        return String.join("\n", points);
    }

    private static Map.Entry<String, Content> entry(String title, String foundation, String method,
                                                     String application, String practice) {
        String overview = "这一章不从术语表开始，而是先解决一个看得见的问题。你会先通过图和最小示例理解“" +
                title + "”，再亲手完成一次操作，最后用边界案例检查自己是否真的学会。";
        return Map.entry(title, new Content(
                overview,
                List.of(
                        "能用自己的话解释：" + foundation,
                        "能跟着示例演示并说明：" + method,
                        "能换一组数据或场景验证：" + application),
                List.of(foundation, method, application),
                practice));
    }

    public record Content(String overview, List<String> objectives, List<String> keyPoints, String practiceTask) {
        public String objectivesText() { return String.join("\n", objectives); }
        public String keyPointsText() { return String.join("\n", keyPoints); }
    }
}
