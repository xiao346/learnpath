package com.learnpath.course;

import java.util.List;
import java.util.Map;

/** Three additional, chapter-specific facts for every lesson. */
public final class ChapterKnowledgeExpansion {

    private ChapterKnowledgeExpansion() {
    }

    private static final Map<String, List<String>> POINTS = Map.ofEntries(
            e("算法与复杂度入门", "渐进分析关注 n 趋近很大时的增长率；Ω 表示下界，O 表示上界，Θ 表示紧确界。", "连续语句复杂度相加取最大项，嵌套循环复杂度通常相乘，互不嵌套的循环不能直接相乘。", "复杂度相同不代表实际耗时相同，数据规模较小时常数、缓存局部性和实现方式仍会影响结果。"),
            e("线性表与顺序存储", "顺序表地址可按 base+i×elementSize 计算，因此支持常数时间随机访问。", "扩容后原数组引用、迭代器或视图可能失效，工程实现要明确复制和引用语义。", "顺序表适合查询多、尾部追加多的场景；中间增删频繁时要比较链式存储的成本。"),
            e("栈、队列与应用", "单调栈维护单调序列，可在线性时间求下一个更大元素或柱状图最大矩形。", "优先队列不遵循普通 FIFO，而是按优先级取元素，通常由堆实现。", "生产者与消费者速度不同时，有限队列需要背压或拒绝策略，避免内存无限增长。"),
            e("二叉树与遍历算法", "二叉搜索树的中序遍历得到有序序列，但退化成链时查找会从 O(log n) 变为 O(n)。", "根据前序和中序可以唯一重建无重复节点二叉树，仅有前序或后序通常不能唯一确定。", "树高决定递归栈深度；极度不平衡的树可能导致栈溢出，需要改用显式栈。"),
            e("图的表示与遍历", "有向图要区分入度和出度，无向图所有顶点度数之和等于边数的两倍。", "拓扑排序只适用于有向无环图，若处理后仍有顶点入度不为零则说明存在环。", "连通分量可通过多次 DFS/BFS 统计；每次从未访问顶点启动一次遍历就发现一个分量。"),
            e("查找算法", "二分查找最容易错在循环不变量、左右边界和 mid 更新，写代码前应明确搜索区间是闭区间还是半开区间。", "开放寻址的装载因子过高会显著增加探测次数，扩容时必须重新散列全部键。", "平衡搜索树保持键有序，既支持 O(log n) 查找，也支持范围查询，这是哈希表不擅长的能力。"),
            e("排序算法", "稳定排序保证相等关键字元素的相对次序，可用于先按次关键字、再按主关键字的多级排序。", "堆排序最坏 O(n log n) 且原地完成，但访问局部性和稳定性通常不如归并排序。", "比较排序存在 Ω(n log n) 下界；计数、桶和基数排序利用键范围等额外条件突破该下界。"),
            e("综合实践与复盘", "数据结构选择应列出核心操作的频率，再用复杂度表比较候选方案的总成本。", "正确性证明可使用循环不变量、数学归纳或反证，至少说明初始化、保持和终止。", "性能测试要固定环境、预热运行、重复采样，并避免把输入生成和日志时间混入算法耗时。"),

            e("数据库系统概述", "数据字典保存表、列、约束和权限等元数据，数据库会用它解释和校验用户操作。", "数据库管理员负责备份恢复、权限、性能和容量，开发者仍需为查询与事务边界负责。", "OLTP 强调短事务和并发更新，OLAP 强调大范围聚合分析，两类负载的数据设计不同。"),
            e("关系模型与关系代数", "关系具有集合语义，理论上元组无序且不重复；SQL 默认允许重复并支持排序。", "除法运算可表达“选修了所有必修课”这类包含全部条件的查询。", "关系代数具有等价变换，例如选择下推能提前减少中间结果，是查询优化器的重要依据。"),
            e("SQL 数据查询", "COUNT(*) 统计行数，COUNT(column) 忽略 NULL；聚合时必须明确缺失值是否应参与口径。", "窗口函数保留原始行并在窗口上计算排名、累计值等，与 GROUP BY 压缩为每组一行不同。", "ORDER BY 是保证结果顺序的唯一可靠方式，没有显式排序时返回顺序不应被依赖。"),
            e("数据库完整性", "外键删除策略 RESTRICT、CASCADE、SET NULL 表达不同业务语义，选择前要评估误删影响。", "CHECK 适合单行条件，跨表余额或库存规则通常应放在事务化业务操作中维护。", "约束名应稳定可读，便于迁移脚本、错误定位和自动化测试准确识别失败原因。"),
            e("关系数据库设计", "第一范式要求属性值原子化，但原子程度取决于业务查询和更新需求。", "无损连接保证分解后的关系自然连接能还原原关系，依赖保持让约束无需连接即可检查。", "反规范化是有证据的性能权衡，必须配套一致性维护策略，不能替代正确的逻辑设计。"),
            e("事务与并发控制", "InnoDB 的一致性读通常使用 MVCC，锁定读 SELECT ... FOR UPDATE 会读取并锁定当前版本。", "可重复读不等于所有数据库行为完全相同，隔离级别的具体实现需要结合产品文档与实验。", "长事务会延迟版本清理并扩大锁范围，应缩短交互时间，避免在事务中等待用户或外部服务。"),
            e("索引优化与课程实践", "覆盖索引让查询所需列都来自索引，减少回表；但索引过宽会增加存储与写放大。", "对索引列使用函数、隐式类型转换或不符合前缀规则的条件，可能让优化器无法有效定位。", "优化目标不仅是单条 SQL 最快，还要考虑写入成本、缓存命中、并发和索引维护总成本。"),

            e("现代 Web 开发概览", "无状态接口便于水平扩展，但身份、幂等和跨请求流程仍需由令牌或持久化状态协调。", "前后端通过 OpenAPI 等契约同步字段与错误，契约变更要保持兼容或提供明确版本。", "同步调用简单直观，异步消息适合削峰和解耦，但会引入最终一致性与重复消费问题。"),
            e("Spring Boot 快速入门", "构造器注入能明确必需依赖并便于测试，通常优于字段注入。", "条件化自动配置会依据类路径、Bean 和配置属性生效，用户自定义 Bean 常可覆盖默认行为。", "Profile 适合环境差异，但大量条件分支会增加维护成本，敏感值始终应由外部安全注入。"),
            e("RESTful API 设计", "PATCH 表达部分更新，PUT 通常表达对指定资源的完整替换；两者应在接口文档中明确字段语义。", "列表接口要统一分页、排序和过滤规则，并限制最大页大小，防止无界查询拖垮服务。", "幂等键可让客户端安全重试创建或支付类请求，服务端必须保存键与处理结果防止重复执行。"),
            e("数据持久化", "实体等同不应仅依赖数据库自增 ID，否则未持久化对象在集合中的行为可能异常。", "事务边界通常放在业务服务层，把一组必须共同成功的数据操作包含在同一事务中。", "批量写入要控制 flush/clear 频率，避免持久化上下文积累大量实体占用内存。"),
            e("参数校验与异常处理", "客户端错误应返回稳定业务错误码，展示文案可以变化，程序不应依赖自然语言判断错误类型。", "校验分组可区分新增与更新规则，但规则过多时应考虑用不同请求对象表达不同用例。", "未知异常要记录关联请求的追踪标识，响应只返回安全信息，服务端日志保留定位所需上下文。"),
            e("身份认证与权限控制", "Cookie 会话需要防范 CSRF，Bearer Token 主要防范泄露和 XSS，两者风险模型不同。", "权限校验必须在每次受保护请求执行，前端隐藏按钮只改善体验，不能构成安全边界。", "退出登录、密码修改和高风险事件应使相关会话失效，并记录可审计的安全事件。"),
            e("Redis 缓存实践", "缓存穿透来自持续查询不存在数据，可用空值缓存或布隆过滤器缓解。", "热点键过期时大量请求同时回源形成击穿，可通过互斥重建、预热或逻辑过期处理。", "Redis 数据不应被默认当作唯一事实来源，除非已明确配置持久化、复制和故障恢复策略。"),
            e("接口测试", "测试名称应描述场景和预期结果，失败时无需阅读实现就能知道哪个行为被破坏。", "外部服务应在单元测试中替身化，在少量集成测试中验证真实契约，避免测试既慢又不稳定。", "断言应关注公开契约而非内部调用细节，否则合理重构也会造成大量无意义失败。"),
            e("部署与综合实战", "应用应能优雅关闭：停止接收新流量、等待进行中请求、释放连接并上报终止状态。", "数据库迁移必须前向兼容滚动部署，例如先加可空列、双写迁移，再删除旧列。", "上线验证要覆盖登录、核心读写、数据库和缓存连接，并预先定义失败时的回滚阈值。"),

            e("网络体系结构", "协议定义语法、语义和同步；接口描述相邻层如何使用服务，两者不能混为一谈。", "封装会增加首部开销，MTU 限制过小时可能触发分片或要求传输层调整报文大小。", "排障应自下而上检查链路、地址、路由、端口和应用，也可按现象从最可能层快速定位。"),
            e("物理层基础", "奈奎斯特准则给出无噪声信道码元速率上限，香农公式进一步考虑信噪比。", "曼彻斯特编码自带时钟但带宽效率较低，不同编码在同步、直流分量和频谱上有权衡。", "时延由传播、发送、处理和排队组成，提高带宽只会直接减少发送时延。"),
            e("数据链路层", "滑动窗口能让发送方在等待确认时继续发送，多帧在途提高高时延链路利用率。", "CSMA/CD 用于传统共享半双工以太网，现代交换式全双工以太网不再发生碰撞。", "MAC 地址只在当前二层广播域转发，跨路由器时帧首部会重写而 IP 目的地址通常保持。"),
            e("网络层与 IP", "子网可用地址数量需扣除网络与广播地址，但点到点和 IPv6 等场景规则有所不同。", "IP 分片会增加丢包代价，IPv6 路由器不进行中途分片，发送端依赖路径 MTU 发现。", "NAT 改写地址和端口缓解 IPv4 短缺，却破坏端到端可达性并增加连接状态。"),
            e("路由选择", "默认路由 0.0.0.0/0 在没有更具体匹配时兜底，最长前缀规则仍优先于管理距离。", "收敛时间描述拓扑变化后全网形成一致可达信息所需时间，期间可能产生环路或黑洞。", "BGP 选择受本地策略、AS 路径等属性影响，互联网路径不一定是物理最短路径。"),
            e("TCP 与 UDP", "TCP 是字节流，没有消息边界，应用协议必须自己定义长度、分隔符或固定格式。", "流量控制保护接收方，拥塞控制保护网络，两者都会限制发送方在途数据量。", "TIME_WAIT 让迟到报文失效并允许重发最后 ACK，高连接率服务需理解其作用而非盲目关闭。"),
            e("应用层协议", "DNS 常先查浏览器、系统和递归解析器缓存，TTL 决定记录可被缓存多长时间。", "HTTP 缓存通过 Cache-Control、ETag 和 Last-Modified 减少传输，强缓存与协商缓存流程不同。", "HTTP/2 多路复用多个流，HTTP/3 基于 QUIC 降低传输层队头阻塞并加快连接建立。"),
            e("网络安全基础", "加密不自动保证身份，必须结合证书、密钥管理和正确的主机名验证。", "数字签名用私钥签名、公开密钥验证，提供完整性、身份认证和不可否认性。", "安全更新、最小权限、分段隔离、备份和监控共同构成防御体系，单一产品无法消除全部风险。"),

            e("Campus Life", "询问信息时先用 Could you tell me... 保持礼貌，再用确认问题确保时间与地点无误。", "schedule、assignment、deadline、facility 是校园高频词，应结合固定搭配而非孤立背诵。", "口语流利度来自意群和停顿，不必追求每个单词同样清晰或完全没有自我修正。"),
            e("Technology and Society", "讨论影响时区分 short-term 与 long-term、individual 与 societal，能让论证维度更完整。", "引用数据要说明来源和范围，单个案例可以说明可能性，却不能直接证明普遍规律。", "反方观点可用 Some may argue... 引入，再用 However... 回应，体现对复杂性的理解。"),
            e("Academic Reading", "摘要通常包含研究背景、问题、方法、结果和结论，先识别这些功能再决定是否精读全文。", "代词 this、they、such findings 的指代关系影响句间理解，应回到前文寻找最近且语义匹配的对象。", "批注应记录段落功能、关键证据和疑问，而不是把原句大段抄写到笔记。"),
            e("Presentation Skills", "图表讲解按“结论—证据—意义”展开，先告诉听众看什么，再解释数字为何重要。", "问答时可先复述问题确认理解，不确定时说明限制并承诺查证，避免编造答案。", "排练要记录实际时长并删减次要内容，每分钟英文演讲通常只容纳有限词数。"),
            e("Critical Writing", "hedging 使用 may、suggest、likely 等控制断言强度，使结论与证据范围一致。", "综合多个来源不是逐篇摘要，而是按主题比较一致、冲突和证据强弱。", "编辑阶段先处理论证结构，再改句子与语法；过早润色可能浪费在最终会删除的段落上。"),
            e("Final Project", "研究问题要可在时间与资料限制内回答，并明确对象、范围和关键概念。", "评价资料要检查作者、出版机构、证据、日期和目的，搜索排名本身不代表可信度。", "项目反思应给出具体证据说明策略何时有效，而不是只写“学到了很多”。"),

            e("人工智能的过去与现在", "图灵测试关注机器行为是否可与人类区分，但不能完整衡量理解、意识或所有智能能力。", "专家系统知识获取成本高且难以覆盖开放环境，统计学习则依赖代表性数据和明确目标。", "生成式模型根据训练分布生成内容，输出具有概率性，同一提示可能得到不同答案。"),
            e("问题求解与搜索", "一致启发式满足三角不等式并保证沿路径 f 值不下降，通常也意味着可采纳。", "重复状态检测需要 closed set，否则图搜索可能在环中反复扩展并浪费大量时间。", "束搜索限制每层保留节点数以换取空间效率，但可能丢弃通向最优解的路径。"),
            e("知识表示", "本体定义概念层次、属性和约束，知识图谱实例则用具体实体填充这些模式。", "前向链从已知事实不断触发规则，后向链从目标反推需要满足的前提。", "同名实体消歧和不同来源实体对齐是知识图谱融合的关键，错误合并会传播到推理结果。"),
            e("机器学习基础", "数据泄漏把预测时不可获得的信息带入训练，离线指标会虚高但上线失效。", "分类阈值影响精确率和召回率，应根据误报与漏报成本选择而非固定使用 0.5。", "交叉验证在多个划分上重复训练评估，样本有时间或群组关系时不能随意随机打乱。"),
            e("神经网络初步", "激活函数引入非线性，否则多层线性变换仍等价于单层线性模型。", "批大小影响梯度噪声、显存和训练速度；训练轮数要结合验证集表现和早停选择。", "输入归一化能改善优化条件，输出层和损失函数则要匹配回归、二分类或多分类任务。"),
            e("自然语言处理", "子词分词在字符与完整词之间折中，能处理未登录词并控制词表规模。", "注意力权重不能自动等同于可靠解释，模型行为仍需反事实、消融和人工评估。", "文本生成评估要结合自动指标、事实核验、安全测试和目标用户的人工评价。"),
            e("负责任的人工智能", "训练数据的合法来源、使用授权和可删除性是治理问题，不只是模型性能问题。", "模型卡记录用途、数据、指标、限制与伦理风险，帮助部署者判断系统是否适合具体场景。", "上线后要监控分布漂移、群体差异和用户投诉，并保留停用或回滚模型的能力。"),

            e("Python 与开发环境", "python -m pip 能确保 pip 属于当前解释器，避免多版本环境下把包装到错误位置。", "锁定直接与传递依赖版本能提高复现性，升级时要通过测试验证兼容性与安全性。", "脚本入口使用 if __name__ == '__main__' 可避免模块被导入时意外执行主流程。"),
            e("数据类型与控制流程", "集合去重与成员判断平均较快，但不应依赖其元素顺序表达业务含义。", "短路求值让 and/or 在结果已确定时不再计算后续表达式，可用于安全的条件访问。", "enumerate 同时取得索引和值，zip 并行遍历多个序列，通常比手工维护下标更清晰。"),
            e("函数与模块", "*args 收集额外位置参数，**kwargs 收集额外关键字参数，公开 API 不应因此隐藏必要参数。", "闭包保留外层作用域变量，装饰器可在不修改函数主体时增加日志、缓存或权限行为。", "模块导入只在首次执行后缓存，循环导入通常说明职责边界需要重新组织。"),
            e("NumPy 数组计算", "布尔索引返回符合条件的元素副本，可组合多个条件，但要用 &、| 并给条件加括号。", "轴 axis 表示被压缩的维度，axis=0 聚合各行得到每列结果，axis=1 聚合各列得到每行结果。", "浮点计算存在舍入误差，比较结果应使用容差函数而不是直接要求完全相等。"),
            e("Pandas 数据处理", "链式赋值可能修改临时对象并产生警告，应使用一次 loc 定位后明确赋值。", "分类类型能减少重复字符串内存并固定类别顺序，适合有限枚举列。", "透视表用于多维聚合，melt 把宽表转成长表，选择结构要匹配后续分析与可视化。"),
            e("数据可视化", "位置和长度通常比面积、角度更易准确比较，因此重要数量关系优先用点或条形表达。", "色彩既要区分类别也要满足灰度和色觉缺陷场景，可用形状、线型或标签提供冗余编码。", "不确定性可用置信区间、误差条或分布表达，隐藏样本量和波动会让图表结论过度确定。"),
            e("综合分析项目", "数据字典应记录字段含义、单位、取值范围和缺失编码，避免团队成员对口径理解不同。", "探索性分析用于提出假设，验证性分析应在独立数据或预先约定方法上检验，避免反复试到显著。", "可复现项目要固定随机种子、依赖和输入版本，并从空输出目录完整运行一次。"),

            e("软件过程与需求", "功能需求描述系统行为，质量属性要给出可度量场景，例如并发数、响应时间和可用率。", "需求追踪矩阵把目标、故事、实现和测试连接起来，变更时可快速判断受影响范围。", "最小可行产品只保留验证核心价值的能力，不等于降低质量或忽略安全底线。"),
            e("Git 核心模型", "git diff 比较工作区与暂存区，git diff --staged 比较暂存区与 HEAD，二者回答不同问题。", "提交包含父提交、作者、时间和树对象，分支合并历史因此可以沿父指针追溯。", ".gitignore 只影响未跟踪文件，已被跟踪的文件需要先从索引移除才会停止纳入提交。"),
            e("分支与合并", "rebase 会产生内容相似但哈希不同的新提交，因此不应改写其他人已经基于其工作的共享历史。", "cherry-pick 把指定提交的变更应用到当前分支，适合有选择地移植修复但可能造成重复提交。", "长期分支偏离越多越难集成，应频繁同步主线并把功能拆成可独立合并的小变更。"),
            e("代码评审与冲突处理", "评审优先检查需求正确性、数据安全和架构风险，再讨论命名与格式等低风险问题。", "自动格式化可以减少无价值风格讨论，让人工评审集中于工具难以判断的设计与行为。", "解决冲突后要检查冲突附近的逻辑组合，因为文本无冲突也可能产生语义冲突。"),
            e("自动化测试与持续集成", "测试金字塔建议大量快速单元测试、适量集成测试和少量关键端到端测试。", "流水线失败必须阻止合并才能形成质量门禁，同时要让失败原因清晰且容易在本地复现。", "依赖缓存与构建产物缓存概念不同，缓存键需包含锁文件或版本信息，避免使用过期依赖。"),
            e("版本发布实践", "预发布版本可用 1.2.0-beta.1 标识，正式版本比较时预发布优先级低于对应正式版。", "发布标签应签名或受保护，确保下载产物能追溯到经过审核的源代码提交。", "回滚数据库比回滚应用更困难，破坏性迁移前必须备份并设计向前修复或兼容窗口。")
    );

    public static List<String> pointsFor(String chapterTitle) {
        List<String> points = POINTS.get(chapterTitle);
        if (points == null) throw new IllegalArgumentException("缺少章节扩展知识点：" + chapterTitle);
        return points;
    }

    private static Map.Entry<String, List<String>> e(String title, String first, String second, String third) {
        return Map.entry(title, List.of(first, second, third));
    }
}
