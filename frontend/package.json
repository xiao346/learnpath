{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "check": "npm run test:lessons && npm run test:depth && npm run test:network && npm run test:database && npm run build",
    "test:lessons": "node scripts/check-lesson-content.mjs",
    "test:depth": "node scripts/check-adaptive-tutorials.mjs",
    "test:network": "node scripts/check-network-tutorials.mjs",
    "test:database": "node scripts/check-database-tutorials.mjs",
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pinia": "^3.0.4",
    "vue": "^3.5.41",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@vitejs/plugin-vue": "^6.0.8",
    "@vue/tsconfig": "^0.9.1",
    "typescript": "~6.0.2",
    "vite": "^8.2.2",
    "vue-tsc": "^3.3.11"
  }
}
