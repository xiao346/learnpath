export type NetworkFigureKind = 'layers' | 'encapsulation' | 'ports' | 'hop' | 'segmentation' | 'handshake' | 'stream' | 'window' | 'closing' | 'signal' | 'frame' | 'subnet' | 'route' | 'dns' | 'trust'
export type TutorialSection = {
  id: string
  title: string
  paragraphs: string[]
  figure?: NetworkFigureKind
  databaseFigure?: 'schema' | 'selection' | 'join' | 'foreign' | 'normalize' | 'transaction' | 'index'
  after?: string[]
  note?: { title: string; body: string }
  code?: { title: string; text: string }
  table?: { headings: string[]; rows: string[][] }
  check?: { question: string; answer: string }
  conceptDiagram?: { label: string; content: string }[]
  visualIndex?: number
  example?: string
  warning?: string
}
export type ChapterTutorial = {
  lead: string
  scenario: string
  sections: TutorialSection[]
  recap: string[]
  sources: { label: string; href: string }[]
  guidedPractice?: {
    title: string
    scenario: string
    steps: { label: string; action: string; explanation: string }[]
    result: string
    tryIt: string
  }
}

/** Independently authored long-form lessons. Missing chapters retain their existing content. */
export const chapterTutorials: Record<string, ChapterTutorial> = {
  '网络体系结构': {
    lead: '从浏览器里的一条请求出发，追踪它如何变成可以在链路上传输的数据，再在另一端还原。先理解每层解决的问题，再记层名。',
    scenario: '假设浏览器需要查询第7本书，服务端收到请求后返回书名。下面始终跟踪这一条请求；图中的地址是文档示例地址，不是可直接访问的真实服务。',
    sections: [
      {
        id: 'why-layers', title: '两台电脑能连上，为什么应用还不能直接通信？',
        paragraphs: [
          '先把“连上”拆开。网线或无线信号解决了信息如何经过介质的问题，却不知道接收者要的是网页、邮件还是语音。即使信息到达了正确电脑，那台电脑同时运行的浏览器、聊天程序和服务器进程，也不能都把这份内容当作自己的数据。',
          '接下来还有几个独立问题：内容用什么格式表达？交给哪一个通信端点？对方在哪个网络？当前这一段链路该把帧交给谁？如果把这些问题全部塞进每个应用，换一种网络介质就可能迫使所有应用重新实现通信。',
          '分层的作用，就是把变化原因不同的工作隔开。应用定义业务消息；传输层提供端点之间的通信服务；网络层根据地址完成跨网络转发；链路层负责当前链路的交付。下层提供服务，上层不必重复实现下层的全部细节。',
        ],
        figure: 'layers',
        after: ['读图时先沿左边向下：请求经过各层处理才能发送。再沿右边向上：接收方识别并去掉对应封装，最终交给应用。横向虚线表示对等层遵守同一种协议，不是说应用层之间还有一根独立的直连网线。'],
      },
      {
        id: 'model-names', title: '四层、五层、七层，为什么教材说法不同？',
        paragraphs: [
          '本章使用常见的 TCP/IP 四层讲法：应用层、传输层、网际层、链路层。某些教材把物理层单列出来，形成五层讲法。OSI 则是七层参考模型，另外区分了会话层和表示层等职责。层数不同，首先是抽象方式不同，不是网络包每经过一本教材就多穿一件外套。',
          '例如，将字符编码、压缩或加密归入哪一层的讲解，不改变这些功能实际需要完成的工作。入门时应先问“谁提供什么服务”，再讨论它在某个模型中叫什么名字。不要把某个实现中的用户态、内核态边界，当成所有协议实现都必须遵守的分层定义。',
        ],
        table: { headings: ['需要解决的问题', '本章采用的层', '观察到的例子'], rows: [
          ['双方怎样理解业务内容？', '应用层', 'HTTP 请求方法、路径、状态码'],
          ['数据交给哪个端点，提供什么交付方式？', '传输层', 'TCP / UDP、端口'],
          ['去哪个网络，下一跳在哪里？', '网际层', 'IP 地址、路由表'],
          ['当前链路如何交付？', '链路层', '以太网帧、MAC 地址'],
        ] },
      },
      {
        id: 'application', title: '应用层先把“我要第7本书”写成双方理解的消息',
        paragraphs: [
          '浏览器里的点击动作不是服务器能直接理解的网络消息。前后端先约定接口，例如路径 /books/7 表示第7本书，GET 表示读取；服务器再按约定返回状态和书籍数据。这是应用协议及接口契约解决的问题。',
          '下面的示例只演示 HTTP/1.1 的可见结构。请求行说明操作和目标，首部补充上下文，空行结束首部。服务器必须按协议解析，不能只看到消息里有数字7就猜它是书号。真实请求可能还包含认证、缓存等首部。',
        ],
        code: { title: '一个简化的 HTTP/1.1 请求（换行按协议使用 CRLF）', text: 'GET /books/7 HTTP/1.1\nHost: library.example\nAccept: application/json\n\n' },
        after: ['应用层决定这段消息的含义，但不负责选择下一台路由器。还要区分协议版本：常见 HTTP/1.1 和 HTTP/2 使用 TCP，HTTP/3 使用基于 UDP 的 QUIC。不能把“HTTP 永远等于 TCP 上的文本请求”当作通用结论。'],
      },
      {
        id: 'transport', title: '传输层为什么需要端口？IP 地址还不够吗？',
        paragraphs: [
          'IP 地址帮助把包送往目标主机的网络位置，却没有直接说明目标主机上的哪个通信端点应接收内容。服务器可能同时提供网站服务和远程登录服务；两者都在同一个 IP 地址上运行，仅靠 IP 无法完成这种区分。',
          'TCP 和 UDP 首部中都有源端口与目的端口。目的端口帮助接收端按协议规则把数据交给匹配的套接字；源端口让对方知道回复发往哪里。端口不是进程 ID，进程也不必只使用一个端口。已建立 TCP 连接通常由本地与远端 IP、端口组成的四元组区分。',
        ],
        figure: 'ports',
        after: [
          '图中两条连接都访问服务器的443端口，但客户端临时端口不同，所以它们不是同一条连接。服务器还可在22端口提供另一项服务。图中的443是常见约定，不代表端口数字本身能证明数据已加密或身份可信。',
          '知道交给谁之后，还要决定提供怎样的交付服务：TCP 提供可靠、有序的字节流；UDP 保留独立数据报的边界，但不自带可靠交付保证。端口解决“交给谁”，可靠性解决“如何交付”，不要把两个问题混在一起。',
        ],
      },
      {
        id: 'ip-link', title: 'IP 找最终目的地，MAC 负责当前这一跳',
        paragraphs: [
          '假设目标服务器不在本地子网，主机通常把 IP 包交给默认网关。此时 IP 包的目的地址仍然是服务器，但承载这个 IP 包的本地以太网帧，目的 MAC 是网关在本链路上的 MAC，而不是远端服务器的 MAC。',
          '路由器收到帧，取出 IP 包，查路由表，确定下一跳和出口，再用出口链路适用的封装发出去。以太网出口需要新的源、目的 MAC。因此链路层首部会随跳转更换；IP 目的地址在普通、不经过 NAT 等地址转换的转发中保持指向最终服务器。',
        ],
        figure: 'hop',
        after: [
          '交换机和路由器由此承担不同工作：交换机学习源 MAC 与端口的对应关系，主要按目的 MAC 转发帧；路由器主要依据 IP 目的前缀决定下一跳。示意图中的线路是逻辑路径，真实路径可以包含更多交换机和路由器。',
          '对本地以太网 IPv4 场景，ARP 可以帮助查询下一跳 IP 对应的 MAC。它通常不是询问全互联网某个远端服务器的 MAC。先判断是否同子网、确定下一跳，再解析这个下一跳的链路地址，顺序不能颠倒。',
        ],
        check: { question: '服务器在另一子网。本机发出的第一帧，目的 IP 和目的 MAC 分别指向谁？', answer: '目的 IP 指向最终服务器，目的 MAC 通常指向本地默认网关。路由器转发到后续链路时重建链路层封装。这个回答假设普通 IPv4 以太网转发，不包含 NAT 等特殊处理。' },
      },
      {
        id: 'headers', title: '封装到底加了什么？为什么不能把数据无限塞进一个包？',
        paragraphs: [
          '封装不是把应用内容改写成另一种业务内容，而是在它外面补上当前层完成工作需要的信息。TCP 首部记录端口、序号等；IP 首部记录 IP 地址等；以太网首部记录 MAC 地址等。接收端读取这些控制信息，才能决定如何处理内部数据。',
          '图里始终保留同一段橙色应用数据，新增字段用不同颜色画在外侧。沿着图从上到下看，能分清“业务内容是什么”与“为了送到那里还需要哪些说明”。如果启用 TLS，HTTP 内容还会先受到加密保护，不能把图当作所有抓包都能直接看到明文的保证。',
        ],
        figure: 'encapsulation',
        after: [
          '链路对单个载荷大小有上限，MTU 就与这个限制有关。例如以太网常见 MTU 为1500字节，指可承载的 IP 包大小，而不是1500字节都能装应用内容。在没有选项的 IPv4 20字节首部和 TCP 20字节首部假设下，最多还剩1460字节 TCP 数据；隧道、选项和不同链路会改变可用大小。',
          'MSS 约束的是 TCP 段中的数据大小，不包含 TCP 和 IP 首部。TCP 分段与 IP 分片是不同层的动作。也不能假定大包到路由器后总会被切开：IPv4 受 DF 等条件约束，IPv6 路由器不执行分片。发送端应结合对端能力和路径条件选择合适大小。',
        ],
        note: { title: '不要把图里的宽度当成实际字节比例', body: '这一幅图重点解释封装包含关系，首部宽度为了可读性放大了。需要计算长度时，使用正文列出的字节数和前提。' },
      },
      {
        id: 'protocol-interface', title: '协议与接口：谁和谁约定，约定哪些内容？',
        paragraphs: [
          '协议是通信的对等方共同遵守的规则。以 TCP 为例，双方要知道字段长什么样、确认号表示什么、什么时候应发送或重传。这分别涉及语法、语义和时序。只知道某个字段占几个字节，还不知道它在状态变化中如何解释，就不能正确实现协议。',
          '接口是组件或层之间使用服务的入口。例如应用调用套接字接口发送字节，这是应用与本机通信实现之间的交互；对端不需要使用同一种编程语言或相同函数名，只需要在网络上遵守兼容协议。',
          '因此，一个应用可用不同语言调用不同形式的接口，却仍通过相同协议通信。反过来，两边都叫 send 的函数，也不意味着它们自动遵守同一种线上消息格式。把接口名与协议规则分开，是理解跨平台互通的关键。',
        ],
      },
      {
        id: 'debugging', title: '网页打不开时，怎样用分层缩小问题范围？',
        paragraphs: [
          '不要把任何错误都统称为“网络不好”。先确认本机链路连接、地址、掩码与网关是否合理，再看名字是否解析到预期地址、目标传输端点能否建立通信，最后检查应用响应。每一步都在验证一个假设，不是机械要求所有问题必须按同一顺序排查。',
          '例如名称解析失败时，浏览器还没有得到目标地址，应用业务代码可能根本没收到请求。若已收到 HTTP 404，至少说明某个 HTTP 服务作出了响应，应进一步核对域名路由、路径和资源是否存在，而不是直接得出网线断开的结论。',
          'ping 不通也不是“TCP 服务一定不可用”的证明：ICMP 可能被限制，目标网站服务却仍能访问。反过来 ping 通，只验证了特定诊断交互，也不能证明目标端口开放、TLS 验证通过或应用返回正确数据。',
        ],
        table: { headings: ['观察到的现象', '可以支持的判断', '还不能证明什么'], rows: [
          ['域名解析没有结果', '先检查解析链路与配置', '不等于业务接口实现有误'],
          ['TCP 连接建立失败', '检查端点、网络与访问控制等', '不能仅凭此确定唯一故障原因'],
          ['TLS 证书验证失败', '检查身份与证书验证条件', '不应通过忽略验证掩盖问题'],
          ['收到 HTTP 404', '有 HTTP 服务返回了未找到响应', '不保证请求到的是预期服务'],
        ] },
      },
    ],
    recap: ['应用协议解释业务内容；传输层把数据交给通信端点；IP 指向目标网络位置；链路层完成当前一跳。', '封装增加控制信息，解封装读取并移除相应控制信息；不要混淆原始内容与每层首部。', '端口、IP、MAC 解决不同范围的寻址问题；诊断时用观察到的证据判断，不从一个工具结果推断全部层都正常。'],
    sources: [
      { label: 'TCP 规范：RFC 9293', href: 'https://www.rfc-editor.org/rfc/rfc9293.html' },
      { label: 'UDP 规范：RFC 768', href: 'https://www.rfc-editor.org/rfc/rfc768.html' },
      { label: 'IPv6：RFC 8200', href: 'https://www.rfc-editor.org/rfc/rfc8200.html' },
      { label: 'HTTP/3：RFC 9114', href: 'https://www.rfc-editor.org/rfc/rfc9114.html' },
    ],
  },
  'TCP 与 UDP': {
    lead: '不只记住“TCP 可靠、UDP 不可靠”。我们实际拆开一段数据，观察丢失、确认和重传，再解释连接、字节流与窗口为什么这样设计。',
    scenario: '本章使用一个3600字节的数据流。为了容易手算，假设每段最多承载1400字节 TCP 数据，并用从1开始的相对数据序号。握手和关闭另用独立数字示例，避免把控制位和数据字节混在一起。',
    sections: [
      {
        id: 'transport-job', title: '已经有 IP，为什么还需要 TCP 或 UDP？',
        paragraphs: [
          'IP 负责跨网络转发数据报，但尽力转发不等于一定到达。路径可能拥堵，包可能丢失；两个包也可能因排队或路径变化而乱序。即使它们全部到达，操作系统还要决定把内容交给哪一个通信端点。',
          'TCP 与 UDP 都用端口参与端点区分。服务器的同一 IP 可以同时提供多项服务；大量客户端也可以同时连接同一个服务端口。以 TCP 为例，通常根据源 IP、源端口、目的 IP、目的端口区分已建立连接，而不是只看目的端口。',
          '端口不是应用在整个互联网中的唯一身份证，也不是进程号。一个进程可以创建多个套接字，一个服务端口也可以服务多条连接。理解这一点，就不会把“服务器只监听一个端口”误解成“只能接待一个用户”。',
        ],
        figure: 'ports',
      },
      {
        id: 'tcp-udp', title: 'TCP 和 UDP 的区别，实际会怎样影响应用？',
        paragraphs: [
          '假设应用连续发送两份独立状态更新。UDP 保留这两份数据报的边界，接收者处理的是一份份报文；但 UDP 本身不保证到达、顺序或去重，应用需要按自己的目标应对这些情况。TCP 则向应用提供有序字节流：底层如何分段、重传或合并，通常不作为应用消息边界交付。',
          '因此，UDP 的“简单”不是业务一定更简单。若要在它之上发送完整文件，通常还需要自行或借助上层协议补充编号、确认、重传、流量与拥塞控制等机制。QUIC 就是基于 UDP 但提供更多能力的传输协议，HTTP/3 使用它；不能据此说 UDP 自身已有这些保证。',
          '也不要把“UDP 一定更快”当作定律。完成任务的时间还受网络、丢包、实现和上层处理影响。实时状态更新有时宁可丢弃迟到的旧状态，也不等它重传；文件传输通常更关注内容完整。选协议时先明确业务需要，不能仅看首部短不短。',
        ],
        table: { headings: ['应用能依赖什么？', 'TCP', 'UDP 自身'], rows: [
          ['交付的基本形态', '有序字节流', '独立数据报'],
          ['丢失后自动恢复', '有确认、重传等机制，连接也可能最终失败', '不提供可靠交付保证'],
          ['保留一次发送的消息边界', '不保留', '保留数据报边界'],
          ['发送速度约束', '具有流量与拥塞控制机制', '应用或上层协议仍需考虑拥塞等要求'],
        ] },
        note: { title: '可靠传输不等于业务已经成功', body: 'TCP 确认到达对端 TCP 的数据，并不能单独证明对端业务已处理、数据库已提交或文件已安全保存。需要这些保证时，应用还要设计自己的响应与恢复语义。' },
      },
      {
        id: 'connection', title: '三次握手：双方究竟在确认什么？',
        paragraphs: [
          'TCP 连接不是在两台电脑之间拉出一条专用电线，而是两端维护一组通信状态：序号、窗口、缓冲区以及连接阶段等。发送数据前，双方需要同步起始序号并交换相关能力，之后才能解释“你收到哪里了”。',
          '看下面的独立示例：客户端初始序号100，服务器初始序号900。SYN 会占用一个序号位置，所以服务器确认客户端 SYN 时填写 ack=101，客户端确认服务器 SYN 时填写 ack=901。这里不是给每个网络包随便加一个编号，而是在建立各方向的序号空间。',
        ],
        figure: 'handshake',
        after: [
          '第一步表达客户端想建立连接及其初始序号；第二步服务器确认客户端 SYN，同时发出自己的 SYN；第三步客户端确认服务器 SYN。服务器收到这个确认，才知道自己的同步信息得到对方确认。两次交互无法完成这条常规路径中的双向确认。',
          '图中未画重传、同时打开等分支，也不表示握手天然提供身份认证。TCP 三次握手与 TLS 握手不是一回事；需要证明服务身份或加密内容时，还要使用相应安全协议。',
        ],
        check: { question: '服务器 SYN 的 seq 改成5000，客户端最后一个 ACK 的 ack 应是多少？', answer: '应为5001，因为 SYN 占用一个序号位置。这个计算只在当前示例中不夹带其他数据的前提下成立；不要把 ack 理解为报文计数器。' },
      },
      {
        id: 'segments', title: '3600字节为什么要分成几段？MSS 限制的是什么？',
        paragraphs: [
          '应用一次提供很长的数据，不意味着网络上必须产生一个同样大的包。TCP 会把字节流组织成段，再交给 IP。分段大小受对端声明的 MSS、路径条件、首部选项及实现策略等影响；MSS 是 TCP 数据部分的上限，不包括 TCP 和 IP 首部。',
          '本例假设选择的数据上限是1400字节，并且发送者按这个大小组织前两段。3600可分为1400、1400、800三段。实际协议不要求每段都必须填满 MSS，这里只是为了让分段和序号能够手算。',
          '使用从1开始的相对数据序号，第一段包含1～1400，第二段包含1401～2800，第三段包含2801～3600。段首的 seq 表示本段第一个数据字节的序号，不是“这是第几段”。TCP 跟踪的是字节位置，所以即使重新划分段边界，仍能描述哪些字节已收到。',
        ],
        figure: 'segmentation',
        after: ['先点击“下一步”看分段，再看中间一段丢失的情况。图中的橙色空缺不是所有数据都消失了，而是1401～2800这一段字节尚未补齐。数字用于描述序号，不是文件中的实际内容。'],
      },
      {
        id: 'ack-retransmit', title: '第三段先到了，为什么确认号还停在1401？',
        paragraphs: [
          '累计确认号表示接收端接下来期望收到的字节序号。若1～1400已连续收到，接收端可确认到1401。现在第三段2801～3600到了，但中间1401～2800仍缺失，累计确认不能直接跳到3601，否则发送端会误以为中间缺口也已补齐。',
          '接收端可能先缓存乱序到达的第三段，而把累计确认维持在1401。等缺失范围补齐后，前3600字节全部连续可用，累计确认才可推进到3601。应用看到的是按序交付，不会因为第三段早到就先拿到文件尾部再拿中间部分。',
          '发送端根据确认反馈和重传计时等机制判断是否需要恢复丢失数据。超时重传与基于重复确认等信号的快速恢复不是同一触发条件；可选的 SACK 还能报告已收到的非连续范围。实际可能重传哪些段取决于算法和反馈，不能说任何丢包都只固定重发一个原始包。',
          '再想一种情况：数据其实已经到达，但 ACK 丢了。发送端重传可能使接收端再次看到同一范围。序号还用于识别重复字节，防止把同样内容作为新数据重复交给应用。可见序号不仅用于排序，也参与恢复与去重。',
        ],
        check: { question: '如果连续收到1～2800，但第三段没到，累计 ack 应是多少？为什么不是2800或3601？', answer: '是2801，表示下一个期望字节。2800是已收到范围的最后一个字节；3601则错误地声称前3600字节都已连续收到。这里使用本章的相对序号且不涉及序号回绕。' },
      },
      {
        id: 'byte-stream', title: '“面向字节流”到底是什么意思？为什么两次写入不一定两次读出？',
        paragraphs: [
          '假设应用先写入 AB，再写入 CD。TCP 关心的是按序传递 A、B、C、D 四个字节，不承诺把前两字节和后两字节作为两条独立消息交付。接收程序可能读到 ABCD，也可能先读到 A、再读到 BCD，或者出现其他满足顺序的拆分。',
          '这不是网络把内容弄乱了，而是应用错误地期待了 TCP 没有提供的边界语义。若接收代码写成“读一次就解析一条消息”，它可能在消息还不完整时解析，也可能把两条完整消息误当作一条。',
        ],
        figure: 'stream',
        after: [
          '解决方法是由应用协议定义边界，例如固定长度、长度前缀或具有明确转义规则的分隔符。长度前缀方案需要先读足长度字段，再根据字段读取完整正文；一次 read 没读够就继续累计，不能把“暂时没凑齐”当作消息格式错误。',
          '接收者也可能一次读入多条消息，这时解析第一条后要保留剩余字节继续解析。长度字段本身需要上限校验，防止异常长度引起过量分配。边界处理是应用协议职责，不是“把 TCP 改成每次只发一个包”就能永久解决的。',
        ],
        code: { title: '长度前缀解析思路（伪代码，省略网络异常与编码细节）', text: '缓冲区 += 新收到的字节\n当缓冲区足够读取长度字段时：\n    读取正文长度，并检查是否超过允许上限\n    如果正文尚未完整到达：等待更多字节\n    否则：取出一条完整消息并交给业务处理\n          保留剩余字节，继续解析下一条消息' },
      },
      {
        id: 'windows', title: '明明没有丢包，为什么发送者也不能一直发？',
        paragraphs: [
          '先考虑接收者。网卡可以很快收到数据，但应用读取或处理数据可能较慢。若发送者无限发送，对端缓冲区会被耗尽。接收窗口 rwnd 表示接收方当前公告的接收能力，用来约束发送，解决的是“接收端能不能装下”。这叫流量控制。',
          '再考虑中间网络。即使对端缓冲区很大，链路和路由器队列也可能过载。发送方维护拥塞窗口 cwnd，根据相应拥塞控制算法与反馈调整发送规模，解决的是“网络能不能承受”。这叫拥塞控制。两个窗口针对的瓶颈不同。',
          '教学上可先用一个简化上界估算还能发送多少：max(0, min(rwnd,cwnd)−在途未确认字节数)。例如 rwnd=4000、cwnd=3000、在途2000，窗口条件下还剩1000字节额度。真实发送还受应用数据、节奏控制和其他机制限制，算出额度不等于此刻必然发送。',
        ],
        figure: 'window',
        after: [
          '切换图中的三个独立场景，观察由哪个窗口成为较小值。接收空间变小时，盲目扩大网络带宽未必有用；网络拥塞时，单纯扩大接收缓冲也不等于消除瓶颈。',
          '慢启动等机制让发送者逐步探测网络承受能力，但不能把所有 TCP 实现都概括为同一条固定增长曲线。这里先建立“根据反馈控制发送”的因果关系，具体算法和阶段转换应在后续拥塞控制专题中单独推演。',
        ],
      },
      {
        id: 'closing', title: '关闭为什么常见四次交互？一边发完，另一边也发完了吗？',
        paragraphs: [
          'TCP 是全双工连接，两个方向都可以传输。客户端没有更多数据要发送，不代表服务器也已经发送完。FIN 表示发送该 FIN 的一方结束自己的发送方向，不是立即抹掉两个方向的全部通信状态。',
          '看独立关闭示例：客户端发送 FIN(seq=501)，服务器确认 ack=502。此时服务器仍可能继续发自己的剩余数据。等服务器也结束发送，再发 FIN(seq=901)，客户端回复 ack=902。FIN 与 SYN 一样占用一个序号位置。',
        ],
        figure: 'closing',
        after: ['四次是常见交互形式，不是任何关闭都必须恰好四个分开的包。若确认和己方 FIN 可以合并，包数可能减少；异常中止还可能使用 RST。先理解两个方向分别结束，再讨论具体包数和状态机，才不会只背“四次挥手”。'],
      },
      {
        id: 'time-wait', title: '最后一个 ACK 发出后，TIME_WAIT 还在等什么？',
        paragraphs: [
          '客户端发出最后的 ACK，不代表服务器一定收到了它。如果这个 ACK 丢失，服务器可能重发自己的 FIN。通常进入 TIME_WAIT 的主动关闭方需要保留足够状态，以便应对这种重传，而不是立即让相关连接状态彻底消失。',
          '等待还帮助旧连接的延迟报文在网络中失效，减少它们干扰后续连接的机会。规范用2MSL描述相关等待，MSL是最大报文段生存时间；具体系统实现与配置不能简单概括成一个适用于所有环境的固定秒数。',
          '因此，看到 TIME_WAIT 不应立即判断为泄漏，更不应在不了解连接复用、关闭模式与资源压力的情况下直接套用调整参数。应先确认是谁主动关闭、连接创建频率以及是否真的造成资源瓶颈。',
        ],
      },
      {
        id: 'end-to-end', title: '最后把全过程串起来：哪一步成功，能证明哪一层成功？',
        paragraphs: [
          '建立连接后，发送应用交出字节；TCP 按条件组织数据段，用序号、确认和恢复机制管理交付；IP 与链路层负责把承载这些内容的数据逐跳转发。接收端 TCP 整理连续字节，再让应用按自己的协议边界解析消息。',
          '发送调用成功，通常不等于对端业务已经收到并完成处理；收到 TCP ACK，也不等于订单已入库。若应用要求“提交一次订单并确认结果”，还需要应用响应、合理超时和重复请求处理等设计。传输层与业务层的成功条件应分别说明。',
          '学习时可以逐项问自己：序号在数什么？ACK 到哪里仍有缺口？读到的字节是否构成完整应用消息？是谁限制了当前发送额度？哪个方向已经结束？能用具体数字回答这些问题，比背一串协议特性更能解释实际现象。',
        ],
      },
    ],
    recap: ['IP 尽力转发；TCP 在其上提供可靠有序字节流，UDP 自身提供独立数据报而不保证可靠交付。', 'TCP 按字节编号，累计确认指向下一个期望字节。段边界、网络包边界和应用消息边界不能混为一谈。', '流量控制关注接收端，拥塞控制关注网络；两者共同限制发送，还要扣除已经在途的未确认数据。', '连接的两个方向可分别结束。TIME_WAIT 有协议目的，传输确认也不等于业务处理完成。'],
    sources: [
      { label: 'TCP 规范与状态机：RFC 9293', href: 'https://www.rfc-editor.org/rfc/rfc9293.html' },
      { label: 'UDP 数据报格式：RFC 768', href: 'https://www.rfc-editor.org/rfc/rfc768.html' },
      { label: 'TCP 拥塞控制基础：RFC 5681', href: 'https://www.rfc-editor.org/rfc/rfc5681.html' },
      { label: 'HTTP/3 与 QUIC：RFC 9114', href: 'https://www.rfc-editor.org/rfc/rfc9114.html' },
    ],
  },
}
