import type { ChapterTutorial } from './chapterTutorials'
import { learningSchema } from './databaseExamples.ts'

const constraints = { label:'PostgreSQL：约束与引用完整性', href:'https://www.postgresql.org/docs/current/ddl-constraints.html' }
const queries = { label:'PostgreSQL：表表达式与连接', href:'https://www.postgresql.org/docs/current/queries-table-expressions.html' }
const transactions = { label:'PostgreSQL：事务', href:'https://www.postgresql.org/docs/current/tutorial-transactions.html' }
const isolation = { label:'PostgreSQL：隔离级别及实现差异', href:'https://www.postgresql.org/docs/current/transaction-iso.html' }
const indexes = { label:'PostgreSQL：联合索引', href:'https://www.postgresql.org/docs/current/indexes-multicolumn.html' }
const explain = { label:'PostgreSQL：阅读执行计划', href:'https://www.postgresql.org/docs/current/using-explain.html' }
const normalization = { label:'多伦多大学CSC343：函数依赖、无损分解与范式', href:'https://www.cs.toronto.edu/~faye/343/f07/lectures/wk12/12_NormalFormsRevised4-up.pdf' }
const scenario = '贯穿示例：101小林、102小周、103小陈；C1是数据库，C2是网络。四条选课分别为(101,C1,90)、(101,C2,70)、(102,C1,NULL)、(103,C1,85)。NULL表示尚未记录成绩，不表示0分。'

export const databaseTutorials: Record<string, ChapterTutorial> = {
  '数据库系统概述': {
    lead:'从“给学生保存成绩”这个任务出发，看看数据库解决了哪些文件本身没有替你解决的问题。再沿一次查询认识表、DBMS、视图与物理存储。', scenario,
    sections:[
      {id:'why-dbms',title:'已经能把成绩写进文件，为什么还需要数据库？',paragraphs:[
        '如果只有你一个人偶尔记录三条成绩，文本文件就能保存内容。但当教师同时改分、学生同时查分时，还要处理谁能看什么、两次修改如何协调、写到一半断电怎么办。把文件写出来只是存储的开始，不是这些问题的答案。',
        '数据库是按规则组织的数据；DBMS是管理这些数据的软件；数据库系统还包含应用、运行环境和相关管理活动。应用通常通过DBMS请求查询或更新，而不是自己计算某条学生记录在磁盘文件中的字节偏移。',
      ],databaseFigure:'schema'},
      {id:'table-row',title:'先说清楚一行代表什么，再决定有哪些列',paragraphs:[
        '学生表的一行代表一名学生，所以101小林只应有一行。选课表的一行代表“一名学生选择一门课程”，因此101可以出现两次：一次对应C1，一次对应C2。看到编号重复，先看表表达的事实，不要立刻把所有重复都删掉。',
        '列描述这种事实的属性，例如学生姓名、班级，或某次选课的成绩。把“90,70”塞进学生行的一个成绩字符串，会丢掉分数与课程的清晰对应；后续查询、约束和更新都更难表达。我们的模型把对应关系单独存成选课行。',
      ],databaseFigure:'join'},
      {id:'three-schemas',title:'同一份数据，可以有不同视角和不同存法',paragraphs:[
        '学生页面可能只呈现自己的成绩，教师页面呈现所教课程的成绩，这些用户视角对应外模式的思想。所有学生、课程、选课怎样组织及相互关联，属于模式所描述的整体逻辑结构。索引、数据页等底层组织属于内模式的讨论范围。',
        '外模式与模式之间的映射说明用户视角如何由逻辑数据获得，模式与内模式之间的映射把逻辑结构联系到底层存储。这是理解三级模式、两级映射的教学框架，不表示任何产品都恰好提供三个同名配置文件。',
      ],table:{headings:['层次','本例看到什么','不应混淆为'],rows:[['外模式','学生成绩视角、教师成绩视角','每个用户复制一个数据库'],['模式','学生—选课—课程及约束','网页表格的颜色'],['内模式','数据页、索引等存储组织','课程的业务含义']]}},
      {id:'independence',title:'数据独立性保护的是哪一类变化？',paragraphs:[
        '如果新增一个索引或迁移存储位置，应用仍能使用原来的逻辑查询，这体现物理数据独立性的目标。调用方关心查什么，DBMS可以改变怎样存、怎样找，不要求每个调用者一起重写磁盘访问代码。',
        '逻辑数据独立性则讨论逻辑结构变化时，能否通过映射或视图等方式保持用户视角。但把name列删掉后仍期待所有旧查询无条件工作，是扩大了这个概念。独立性需要设计兼容边界，并不保证任意破坏性改表都没有影响。',
      ],check:{question:'给成绩表增加一个索引，却不修改SELECT语句，主要体现哪种独立性？',answer:'主要是物理数据独立性：访问路径改变，逻辑查询仍然成立。索引是否改善性能是另一个问题，还应看执行计划、数据量和查询条件。'}},
      {id:'query-journey',title:'一条查询从输入到结果，中间发生什么？',paragraphs:[
        '应用提交查询后，DBMS需要识别语法与对象、检查相关权限，再考虑适合的执行方式。相同的结果可能通过顺序扫描、索引定位或不同连接次序得到；查询语句通常描述目标，不逐个规定机器必须执行的循环。',
        '执行器取到数据并计算结果后，把结果交回调用方。返回三行并不说明只访问了三行，更不说明这三行已经按某种业务顺序排列。要了解成本看执行计划，需要确定排序就显式写ORDER BY。',
      ],code:{title:'读取一名学生的已登记成绩',text:'SELECT course_id, score\nFROM enrollments\nWHERE student_id = 101\nORDER BY course_id;\n-- C1 | 90\n-- C2 | 70'}},
      {id:'working-dataset',title:'用一套小数据开始，先能核对再扩大规模',paragraphs:[
        '下面是本课程共享的建表与样例数据，用来在独立练习库中复现结果。学生和课程先存在，选课再通过外键引用它们；同一个学生与同一门课程的组合不能重复。本例暂不建模重修、学期或同课多次考试。',
        '代码采用常见SQL写法，课程说明以PostgreSQL的相应行为为参照。只应在新建、可丢弃的练习环境中运行；不要把示例表名直接用于已有业务库。执行后先检查三名学生、两门课程、四条选课是否齐全。',
      ],code:{title:'共享练习数据：新建空白练习库后执行一次',text:learningSchema}},
    ],recap:['先明确一行代表的事实，再设计属性与关联。','DBMS统一管理查询、约束、并发与存储，不等同于一个数据文件。','模式分层帮助隔离变化，但不意味着任意改表都能保持兼容。'],sources:[constraints,queries,explain],
  },
  '关系模型与关系代数': {
    lead:'把关系运算理解成对一批具体行的操作：筛掉哪些、保留哪些列、怎样匹配另一张表。先手算结果，再把符号与SQL对应起来。',scenario,
    sections:[
      {id:'relation-key',title:'关系、元组、属性，说的分别是什么？',paragraphs:[
        '把选课表看成一个关系，一条(101,C1,90)记录就是一个元组，学号、课程号、成绩就是属性。属性有取值范围和业务含义：成绩90是一个数值，C1是课程标识，两者不能因为都能显示成字符就随意交换。',
        '候选键是能够唯一标识元组、且不能再去掉属性仍保持这种能力的属性组合。本例学号不能单独标识选课，课程号也不能；二者组合在“不允许重复选同一课”的规则下可以。选择其中一个候选键作为主键，不会把其他候选键的意义抹掉。',
      ],table:{headings:['属性组合','能否唯一标识本模型的选课','原因'],rows:[['学号','不能','101选了C1和C2'],['课程号','不能','C1被三名学生选中'],['学号＋课程号','可以','模型规定同一组合不重复']] }},
      {id:'select-project',title:'选择筛行，投影留列：一步一步看结果',paragraphs:[
        '“找成绩不低于80的选课”先检查每一行：90通过、70不通过、NULL不是已知的合格成绩、85通过。因此选择后只剩(101,C1,90)和(103,C1,85)，此时每行的三项属性仍在。',
        '若问题只需要学号，再做投影，留下101和103。把两个动作分开，就能解释为什么条件写成绩，但最终结果可以不显示成绩。图中的选择符号σ与投影符号π是运算记号，不是要求你直接在SQL里输入它们。',
      ],databaseFigure:'selection',code:{title:'对应SQL',text:'SELECT DISTINCT student_id\nFROM enrollments\nWHERE score >= 80\nORDER BY student_id;\n-- 101\n-- 103'}},
      {id:'set-bag',title:'为什么关系代数的集合与SQL结果可能不一样？',paragraphs:[
        '经典关系模型按集合理解关系，不保留重复元组。把选课关系投影到课程号，只会留下C1和C2。普通SQL的SELECT course_id却保留重复值，在这份数据上会产生四行，其中三行是C1。',
        '要去重可以用DISTINCT，但也不能为了让结果“看起来干净”到处加它。一对多连接产生多行可能正是正确业务结果，随意去重会隐藏信息。集合运算与SQL的多重集语义要在每道题中先约定清楚。',
      ],check:{question:'SELECT student_id FROM enrollments为什么产生四行，而不是三行？',answer:'它保留每条选课的学号，101出现两次，102和103各一次。需要“选过课的不同学生编号”才使用DISTINCT，得到101、102、103。'}},
      {id:'cartesian-join',title:'连接不是并排贴表，而是按条件组合行',paragraphs:[
        '三名学生与四条选课若做笛卡尔积，会得到3×4=12种配对，其中大多数把姓名与别人的成绩错误地放在一起。加上students.id=enrollments.student_id条件，才留下身份对应的四种配对。',
        '图中101小林会与两条选课匹配，于是结果出现小林-C1和小林-C2两行。连接结果的行数由匹配关系决定，不能默认等于左表行数。相同姓名也不是可靠连接条件，因为不同学生可能同名。',
      ],databaseFigure:'join'},
      {id:'union-difference',title:'并、交、差怎样对应“选了哪些课”？',paragraphs:[
        'C1的学生集合是{101,102,103}，C2的学生集合是{101}。二者并集仍是{101,102,103}；交集是{101}；C1减去C2得到{102,103}，表达“选了C1但没选C2”。差集有方向，反过来就变成空集。',
        '参与集合运算的结果必须在列数、位置和类型上兼容，不能把姓名集合与分数集合直接混在一起解释。SQL中UNION与UNION ALL在去重上不同；INTERSECT、EXCEPT的具体支持与细节应查看使用的数据库。',
      ],table:{headings:['问题','运算','本例结果'],rows:[['至少选一门','C1 ∪ C2','101、102、103'],['两门都选','C1 ∩ C2','101'],['只在C1集合中','C1 − C2','102、103']]}},
      {id:'division',title:'“选了所有指定课程”为什么不是简单数总行数？',paragraphs:[
        '指定课程集合是{C1,C2}时，逐个检查学生：101同时拥有两项关联，102和103都缺C2，所以答案只有101。关系除法刻画的就是这种“对指定集合中的每一个对象都满足关联”的问题。',
        '实际SQL可先限制到指定课程，再按学生统计不同课程数，或使用“不存在任何缺少的指定课程”这种双重否定。直接数所有选课总行数可能被其他课程、重复记录或空的指定集合影响，必须先明确范围与边界。',
      ],code:{title:'指定集合固定为C1、C2时的一种写法',text:"SELECT student_id\nFROM enrollments\nWHERE course_id IN ('C1','C2')\nGROUP BY student_id\nHAVING COUNT(DISTINCT course_id) = 2;\n-- 101"}},
    ],recap:['选择改变行集合，投影改变属性集合。','连接先看匹配条件及关系基数，不按两张表的行号拼接。','SQL默认保留重复结果；去重、集合运算和全称条件都要明确语义。'],sources:[queries],
  },
  'SQL 数据查询': {
    lead:'不从背关键字顺序开始，而是先预测结果。沿四条选课做筛选、连接、分组、排序，专门解释NULL和重复行这些容易“能运行却算错”的地方。',scenario,
    sections:[
      {id:'sql-setup',title:'先准备能手算的表，再写第一个查询',paragraphs:[
        '如果不知道表里有什么数据，就很难判断一个能运行的SQL是否正确。先在独立练习库执行下方共享脚本，确认学生3行、课程2行、选课4行。所有后续查询都以这份初始数据为依据，不依赖你本机的真实学生信息。',
        '先写出问题“101同学选了哪些课程，成绩是多少”，再确定来源enrollments、条件student_id=101和输出列course_id、score。SELECT描述希望得到的结果，数据库仍可选择不同物理执行方式。',
      ],code:{title:'空白练习库的建表与数据',text:learningSchema}},
      {id:'where-null',title:'WHERE只留下条件为真的行，NULL不等于0',paragraphs:[
        'score>=80保留90和85两条记录。NULL表示未知或尚未记录，拿它与80比较不能得出真；WHERE不会保留这个结果。这与成绩确实为0不同，0明确不及格，NULL则连是否参加考试都不能仅凭它判断。',
        '查未登记成绩应使用IS NULL，而不是score=NULL。组合条件时还要明确括号：课程条件与成绩条件之间的AND、OR不是按中文读句子的习惯自由组合，括号能直接表达预期逻辑。',
      ],databaseFigure:'selection',code:{title:'两种不同问题',text:"SELECT student_id FROM enrollments\nWHERE score >= 80 ORDER BY student_id;\n-- 101、103\nSELECT student_id FROM enrollments\nWHERE score IS NULL;\n-- 102"}},
      {id:'join-query',title:'把编号换成姓名，为什么结果仍有四行？',paragraphs:[
        '选课表只保存student_id，并不重复保存姓名。查询时把学生表s与选课表e按编号相等连接，就能同时输出姓名、课程号和成绩。小林的两门课程都是独立事实，因此在结果中占两行，不应随意合并成一行。',
        'INNER JOIN只保留匹配行；LEFT JOIN还保留左侧没有匹配的学生，并在右侧列上补NULL。若又在WHERE里要求右表score>=80，无匹配行会被过滤；想保留所有学生并仅关联合格记录，可把条件放入ON并仔细确认业务目标。',
      ],databaseFigure:'join',code:{title:'先核对连接条件，再核对结果行数',text:'SELECT s.name, e.course_id, e.score\nFROM students AS s\nJOIN enrollments AS e ON e.student_id = s.id\nORDER BY s.id, e.course_id;\n-- 小林 C1 90；小林 C2 70；小周 C1 NULL；小陈 C1 85'}},
      {id:'group-count',title:'COUNT(*)、COUNT(score)、AVG(score)分别在数什么？',paragraphs:[
        '四条选课中有三条已登记成绩，因此COUNT(*)=4，COUNT(score)=3。SUM(score)=90+70+85=245；AVG(score)按三条非空成绩计算，得到245/3，而不是把NULL偷偷当0后除以4。是否把缺考记0是业务规则，不应由聚合函数替你决定。',
        '按课程分组后，C1有3条选课、2条有效成绩，平均为(90+85)/2=87.5；C2有1条选课，平均70。WHERE先筛原始行，HAVING筛聚合后的组。若先WHERE score>=80，会先删去不合格和空成绩，统计目标就已经改变。',
      ],code:{title:'同时显示人数和已出成绩人数，避免把分母混淆',text:'SELECT course_id, COUNT(*) AS enrolled,\n       COUNT(score) AS graded, AVG(score) AS mean_score\nFROM enrollments\nGROUP BY course_id\nORDER BY course_id;\n-- C1 | 3 | 2 | 87.5\n-- C2 | 1 | 1 | 70'}},
      {id:'order-limit',title:'想要稳定的前几名，排序必须能处理并列',paragraphs:[
        '没有ORDER BY时，不应依赖当前屏幕上恰好出现的行顺序。按成绩降序取前两条，先明确NULL是否参与；本例先过滤score IS NOT NULL，再按score DESC排列，得到90、85。',
        '当两名学生同分时，只按分数仍不足以确定相对顺序，可以追加学号和课程号作为稳定的并列规则。分页也要考虑数据在两次请求之间新增或修改的情况；固定排序是前提，不代表OFFSET分页天然拥有跨请求不变快照。',
      ],code:{title:'已登记成绩中取稳定的前两条',text:'SELECT student_id, course_id, score\nFROM enrollments\nWHERE score IS NOT NULL\nORDER BY score DESC, student_id, course_id\nLIMIT 2;\n-- 101 C1 90；103 C1 85'}},
      {id:'exists',title:'只问“有没有选课”，不必把所有选课展开',paragraphs:[
        '如果问题只是找选过C2的学生，可以对每名学生使用EXISTS检查是否存在相应选课。它表达的是存在性，不要求把匹配的每条子记录都投影成结果行，所以特别适合避免在一对多关系上先扩行再去重。',
        '查询后应拿101作为正例、102和103作为反例核对，再补一个没有任何选课的学生。边界数据能暴露条件放错位置的问题；数据库返回成功状态只证明语句可执行，不证明它回答了你真正想问的问题。',
      ],code:{title:'存在性查询',text:"SELECT s.id, s.name FROM students AS s\nWHERE EXISTS (\n  SELECT 1 FROM enrollments AS e\n  WHERE e.student_id=s.id AND e.course_id='C2'\n);\n-- 101 小林"},check:{question:'C1的平均成绩为什么是87.5，而不是58.33？',answer:'AVG(score)忽略NULL，分母是两条有效成绩，(90+85)/2=87.5。若业务明确要求未登记按0计算，才考虑有意识地转换数据，且要说明统计口径改变了。'}},
    ],recap:['先写出预期行和列，再编写查询并核对。','NULL、重复行、连接基数和聚合分母会直接影响正确性。','排序要处理并列；能运行不等于业务语义正确。'],sources:[queries,constraints],
  },
  '数据库完整性': {
    lead:'把“数据不能乱写”变成数据库可以检查的规则：谁必须唯一、谁不能缺失、谁必须引用已存在的对象，以及哪些业务条件还需要事务来配合。',scenario,
    sections:[
      {id:'integrity-rules',title:'前端已经校验，为什么数据库还要再检查？',paragraphs:[
        '网页可能拒绝120分，但导入脚本、后台任务或另一套客户端也能写入数据。如果正确性只依赖某一个界面的判断，就很难保证其他入口遵守同样规则。数据库约束把适合表达的规则放到共同写入边界。',
        '约束并不替代友好的应用提示。应用应提前解释错误，数据库负责最后守住一致的数据条件；发生冲突时，把数据库错误翻译成可理解的业务反馈，而不是把整个堆栈展示给新手用户。',
      ],table:{headings:['想约束什么','适合的基础机制','本例'],rows:[['身份不重复','PRIMARY KEY / UNIQUE','学生编号唯一'],['必要字段不能缺失','NOT NULL','选课必须有学号'],['取值满足规则','CHECK','成绩为0到100或未登记'],['引用对象存在','FOREIGN KEY','选课引用已有学生']]}},
      {id:'primary-unique',title:'主键标识一行，联合主键标识一种组合',paragraphs:[
        'students.id主键使101只代表一名学生，而不是让多个同名小林共享一个编号。姓名不能作为可靠身份依据，因为业务允许同名。主键约束与展示顺序无关，它不代表“这行永远在表格第一行”。',
        '选课主键是(student_id,course_id)，限制的是两列组合不重复。101-C1与101-C2可以同时存在，但再次插入101-C1应失败。若业务允许跨学期重修，就必须扩展模型，如加入学期或教学班，而不是直接删除约束后不再识别选课身份。',
      ],code:{title:'同一学生可以选不同课程，不能重复同一组合',text:'PRIMARY KEY (student_id, course_id)'}},
      {id:'check-null',title:'CHECK(score BETWEEN 0 AND 100)为什么还允许NULL？',paragraphs:[
        '本课程允许尚未录入成绩，所以score可以为NULL。以PostgreSQL的CHECK规则为例，条件为真或未知时均不拒绝；120明确不满足范围会失败，NULL产生未知则不会被这个CHECK单独拦住。',
        '如果规则变成“每条记录必须已经有成绩”，需要再加NOT NULL。不要指望范围检查自动代替非空检查；也不要把空字符串、0和NULL作为同一种缺失情况混用，否则检索和统计的含义会不断变化。',
      ],code:{title:'允许未登记，但已登记成绩必须合法',text:'score INTEGER CHECK (score BETWEEN 0 AND 100)\n-- NULL允许；0允许；100允许；120拒绝'}},
      {id:'foreign-reference',title:'外键不是“这两列名字相同”，而是引用必须成立',paragraphs:[
        '选课student_id=101时，数据库需要在被引用的学生键中找到101。1099不存在，所以该非空引用应被拒绝。仅仅把字段命名为student_id并不会自动创建检查，必须在模型或迁移中定义实际外键。',
        '本例外键列也声明NOT NULL，所以每条选课都要指向一名学生。一般模型中的可空外键可能表示可选关系，NULL与不存在的非空编号不是同一种情况。理解这一点，才能分别设计“必须关联”和“关联时必须有效”。',
      ],databaseFigure:'foreign'},
      {id:'delete-policy',title:'被选课引用的学生要删除，应该发生什么？',paragraphs:[
        '删除学生101会影响其C1、C2两条选课。可以拒绝删除直到相关引用处理完毕，也可以明确采用级联删除；某些模型还允许置空，但要求相应列可空且业务能解释孤立记录。不同策略不是数据库替业务做出的免费决定。',
        '学生离校是否应保留历史成绩，是业务与保留策略问题。若需要历史记录，直接级联删除可能不符合要求，应考虑状态字段、归档或其他模型。练习时只在可丢弃样例库观察约束效果，不要用真实学生记录试删除。',
      ],check:{question:'给选课加外键以后，能否保证学生姓名绝不会为空？',answer:'不能。外键保证相应引用成立，姓名是否为空由学生表name列的NOT NULL等规则负责。每个约束解决不同条件，不能用“有外键”替代所有数据检查。'}},
      {id:'concurrent-constraints',title:'两个人同时抢最后一个名额，仅靠先查再写够吗？',paragraphs:[
        '假设剩余名额为1，两个请求几乎同时查询，都看到1，然后分别插入报名记录。每个请求单独看来都检查过，但组合结果可能超出容量；完整性问题不只发生在单行字段格式上，还发生在并发交错中。',
        '这类规则通常需要把检查与写入组织成合适事务，并使用锁、带条件的原子更新或满足目标的隔离策略。跨多行的业务规则不能随意当作普通行级CHECK来实现。先明确不变量，再用并发用例证明方案有效。',
      ]},
    ],recap:['主键、非空、检查和外键分别守住不同条件。','约束必须真实定义在写入路径上，不是字段名字或页面提示。','涉及多行与并发的规则，还需要正确的事务设计。'],sources:[constraints,isolation],
  },
  '关系数据库设计': {
    lead:'不把“拆表越多越好”当结论。先观察重复事实怎样造成更新、插入和删除异常，再用函数依赖解释拆分依据，并核对拆开后能否正确还原。',scenario,
    sections:[
      {id:'design-facts',title:'先问每条记录在陈述哪一种事实',paragraphs:[
        '如果把学号、姓名、课程号、课程名、成绩放在同一张选课表，小林选两门课就要重复写姓名。这里混在一起的是学生身份事实、课程事实与选课成绩事实，三者不总在同一时刻发生变化。',
        '建模时先写下业务规则：一个学号标识一个学生，一门课程有课程名，一个学生对一门课程在本模型中只有一条选课。不允许重修只是当前教学假设，不是所有学校都应该遵守的现实规则。',
      ],databaseFigure:'normalize'},
      {id:'anomalies',title:'冗余为什么不只是“多占一点空间”？',paragraphs:[
        '小林改名时如果只改C1行，没有改C2行，同一个101就出现两个名字，这是更新异常。新开一门还没人选的课程若必须依附选课行才能录入，可能出现插入困难；删除最后一条选课又可能顺带丢掉课程信息，这是删除异常。',
        '三个问题的共同根源是独立事实被迫重复或依附其他事实。拆表的目标是让每类事实有清楚的维护位置，而不是仅仅减少几个字节。若保存的是“当时使用的历史姓名”，又是另一种事实，需要单独命名和解释。',
      ],table:{headings:['异常','合并表里的表现','拆分后维护位置'],rows:[['更新','改姓名要改多行','学生表的一行'],['插入','无人选的新课程难独立登记','课程表独立建行'],['删除','删最后选课顺带失去课程描述','选课与课程生命周期分开']]}},
      {id:'functional-dependency',title:'函数依赖来自规则，不是当前样本恰好没有重复',paragraphs:[
        '学号→姓名表示：在符合规则的数据状态中，同一个学号不能决定出两个不同的当前姓名。它不要求姓名反过来也能唯一决定学号。两个学生同名是允许的，所以姓名→学号并不是本模型的有效依赖。',
        '(学号,课程号)→成绩表示必须知道两部分，才能确定哪一次选课的成绩。只知道101，无法在90与70之间确定一个。当前样本里每个分数恰好不同，也不能据此宣布成绩→学号，因为下一次考试完全可能同分。',
        '能决定关系全部属性的属性集合叫超键；去掉任何一项就不再具备这种能力的超键，才是候选键。若依赖X→A里的A本来就在X中，例如(学号,课程号)→学号，这种依赖是平凡的；范式讨论重点是右侧带来额外事实的非平凡依赖。',
      ],check:{question:'当前三个学生姓名都不同，能否直接认定姓名是候选键？',answer:'不能。候选键需要业务约束支持，而不是仅看当前样本无重复。如果系统允许同名，未来合法数据就可能打破这种偶然唯一性。'}},
      {id:'normal-forms',title:'1NF、2NF、3NF分别在排除什么问题？',paragraphs:[
        '入门时先把1NF理解为属性值在选定模型中按单一值处理，不在一个字段里塞入可重复的课程清单。满足1NF后，2NF要求非主属性不部分依赖某个候选键。本例姓名只依赖复合键中的学号，因此放在选课关系中会造成部分依赖。',
        '3NF对每个非平凡函数依赖X→A，要求X是超键或A是主属性。常见入门情形可以理解为消除非键事实通过另一非键事实间接依赖键的冗余。例如学生表再反复保存“班级→班主任”，就要审视班级事实是否应独立维护。主属性指属于某个候选键的属性，不能简单等同于选定主键的某一列。',
      ],note:{title:'范式不是编号越高越适合所有业务',body:'BCNF进一步要求每个非平凡函数依赖的决定因素都是超键。实际分解还要检查无损连接、依赖保持及查询成本；不能仅凭背出范式名称就证明设计正确。'}},
      {id:'lossless',title:'拆开以后，重新连接会不会凭空造出记录？',paragraphs:[
        '把合并表拆成学生(id,name)与选课(student_id,course_id,score)，通过学生唯一编号连接，可以把姓名补回每一条选课。对于这种二元分解，共同属性能决定一侧关系的全部属性，是判断无损连接的重要条件。',
        '如果错误地只保留“学号集合”和“成绩集合”，却丢掉它们的对应关系，再组合就会把每名学生与每个成绩配对，产生原来不存在的事实。无损不是文件总大小没变，而是按约定连接后能还原原关系，不多造也不丢失元组。',
      ],databaseFigure:'join'},
      {id:'tradeoffs',title:'什么时候保留冗余，怎样避免它再次失控？',paragraphs:[
        '有些系统为了读取性能保存汇总或冗余字段，但必须说明权威数据在哪里、更新怎样同步、失败怎样补偿。例如缓存的课程平均分应标明计算规则和刷新方式，不能与每条成绩都被当成各自独立的真相。',
        '还要区分冗余与历史快照。订单创建时的收货地址即使与客户当前地址不同，也可能是有意保留的历史事实。设计时写清字段的时间语义，再用改名、改分、删除与新增等场景检查模型，胜过机械地把所有重复文本抽成一张表。',
      ]},
    ],recap:['把独立事实混存会带来三类异常，拆分应有函数依赖依据。','唯一性和函数依赖来自业务规则，不能只观察当前几行。','分解后检查无损与依赖保持；保留冗余必须说明维护责任。'],sources:[normalization,constraints,queries],
  },
  '事务与并发控制': {
    lead:'用可回滚的转账演示理解原子性，再把两个人同时修改同一行的动作排成时间表。重点不在背ACID，而在解释什么状态可以成为最终结果。',scenario:'本章另用独立的账户示例：A有500，B有300，转账100。仅模拟同一数据库内的数据更新，不涉及真实付款或外部支付系统。',
    sections:[
      {id:'atomic-transfer',title:'扣款成功、加款失败，为什么不能把一半当成功？',paragraphs:[
        '目标是A从500变400，B从300变400，合计始终应为800。程序先执行扣款再执行加款，内部不可避免经历A=400、B=300的阶段。如果把这一步单独保存成转账成功结果，就把100从业务账目中丢掉了。',
        '事务把相关更新组织成一个整体。点击图中的下一步，到中间状态时选择回滚，可以看到两个账户恢复初始值；继续到提交，则整体保留400、400。本图假设其他事务不会脏读这些未提交变化，不代表任意数据库的任意隔离设置都相同。',
      ],databaseFigure:'transaction'},
      {id:'acid',title:'ACID四个词，各自在保护哪一件事？',paragraphs:[
        '原子性关心整体成功或撤销，隔离性关心并发事务相互观察和影响的规则，持久性关心已确认提交在相应故障模型下是否保留。一致性要求事务把满足约束的数据带到仍满足约束的状态，依赖正确业务逻辑与数据库机制共同实现。',
        '如果程序给B加了90而不是100，两条更新即使原子提交，也不能自动满足“转账总额守恒”。同样，数据库事务不能自动撤回已经发出的邮件。外部副作用需要另行设计可靠通知、去重或补偿，不能仅用BEGIN把所有系统绑成一个本地事务。',
      ],table:{headings:['性质','本例关注点','不能替代'],rows:[['原子性','两账户更新整体生效或撤销','金额逻辑的正确编写'],['一致性','约束与业务不变量仍成立','所有业务规则自动推导'],['隔离性','并发读取与写入的可见性','完全不用考虑冲突'],['持久性','已确认提交的保存保证','独立备份与灾难恢复']]}},
      {id:'transaction-code',title:'BEGIN与COMMIT之间，还要检查业务结果',paragraphs:[
        '下面在独立练习库建立两个账户，然后用带条件的扣款防止余额不足。扣款语句成功执行不等于真的扣到一行：如果条件不满足，影响行数可能为0。应用必须检查结果，失败就回滚，不能继续给B加款。',
        '目标账户也必须存在，并核对加款影响行数。真实代码还要处理异常、超时、事务连接归属以及重试；不要把示意SQL中的COMMIT理解成无条件应该执行。每个失败出口都应说明事务怎样结束。',
      ],code:{title:'教学流程；分支由应用或存储过程判断',text:"CREATE TABLE accounts (id VARCHAR(10) PRIMARY KEY, balance INTEGER NOT NULL CHECK(balance>=0));\nINSERT INTO accounts VALUES ('A',500),('B',300);\nBEGIN;\nUPDATE accounts SET balance=balance-100\nWHERE id='A' AND balance>=100;\n-- 应用检查影响行数为1，否则ROLLBACK并停止\nUPDATE accounts SET balance=balance+100 WHERE id='B';\n-- 再检查影响行数为1；所有检查成功才COMMIT\nCOMMIT;"}},
      {id:'lost-update',title:'两个人都读到500，为什么最后可能只减去一次？',paragraphs:[
        '考虑一种错误实现：T1读到500，在应用中算出400；T2也读到500，算出450；T1写入绝对值400，T2再写入旧结果450。最终450丢失了T1扣除100的影响，而预期两次扣款后应是350。',
        '数据库和隔离级别可能阻塞、拒绝或检测这种冲突，但不能默认所有读改写代码天然安全。可使用合适的原子更新、版本条件或锁策略；若使用版本号，更新条件应检查旧版本，影响行数为0时重新读取并处理冲突，不能把旧结果原样再提交。',
      ],table:{headings:['时刻','T1','T2'],rows:[['1','读500',''],['2','','读500'],['3','算出并写400',''],['4','','根据旧读数写450'],['最终','T1影响被覆盖','不是期望的350']]}},
      {id:'isolation-levels',title:'脏读、不可重复读、幻读，看的是不同变化',paragraphs:[
        '读到另一个事务尚未提交的值叫脏读；同一事务重复读取某行，发现其他事务已提交的修改使它变化，是不可重复读；重复按条件查询，符合条件的行集合因其他提交而变化，是幻读。它们不是同一问题换了三个名字。',
        '具体保证要按产品看。PostgreSQL的Read Committed可在不同语句间看到新提交；Repeatable Read采用事务快照并提供比标准最低要求更强的部分保证，但仍可能出现序列化异常。Serializable要求已提交事务的效果可解释为某种串行次序，应用仍可能需要重试被拒绝的事务。',
      ],check:{question:'同一事务第一次查C1有3人，别人提交新增选课后第二次查有4人，这属于哪类现象？',answer:'描述的是幻读：同一条件下的结果行集合变化。是否能发生要看隔离级别、查询方式和数据库实现，不能单凭使用了BEGIN就排除。'}},
      {id:'locks-retry',title:'加锁为什么可能互相等待，MVCC又省掉了什么？',paragraphs:[
        'T1先锁A再等B，T2先锁B再等A，就形成循环等待。统一资源访问顺序可以降低这类死锁风险，数据库也可能检测死锁并中止一个事务。被中止后应从事务边界考虑重试，并限制重试次数，而不是只重放最后一条语句。',
        'MVCC通过多版本与可见性规则，让某些读取不必等待当前写入完成。但它不是“完全没有锁”：写写冲突、约束和其他操作仍可能等待或失败。先用两会话时间线核对读取版本、锁持有和提交顺序，再判断具体方案解决了什么并发问题。',
      ]},
    ],recap:['事务内部可能有中间状态，最终提交或回滚必须整体解释。','ACID不能替你写对金额规则，也不自动覆盖外部副作用。','并发方案需要考虑可见性、冲突与重试，不能仅看是否使用事务。'],sources:[transactions,isolation],
  },
  '索引优化与课程实践': {
    lead:'先在小型B+树上走一遍查找路径，再回到SQL看联合索引、回表与执行计划。优化的目标是降低真实成本，而不是让计划里一定出现“索引”两个字。',scenario,
    sections:[
      {id:'why-index',title:'没索引时可能逐行检查，有索引时多维护一条访问路径',paragraphs:[
        '查score>=80时，如果没有合适访问路径，数据库可能扫描很多行后再过滤。索引把某些键按特定结构组织，帮助更快定位候选记录。但它不会改变查询应该返回哪些正确结果，只改变取得结果的可能方式。',
        '索引需要空间，插入、删除和修改相应字段时也要维护。给所有列都建索引可能使写入更贵，还增加缓存压力。先列出常用查询、返回比例和更新模式，再决定哪些访问路径值得额外维护。',
      ],table:{headings:['操作','可能获得的收益','需要付出的成本'],rows:[['按键定位','缩小扫描范围','索引页空间'],['范围查询','利用有序结构','选择性低时未必划算'],['插入或更新','保持后续查找可用','同步维护相关索引']]}},
      {id:'btree-path',title:'B+树不是每个节点只有两个孩子的二叉树',paragraphs:[
        '一页里可以容纳多个键与子指针，因此B+树通常具有较高扇出，用较少层数组织大量记录。下图是刻意缩小的两层结构：根里的20、40负责分隔范围，叶页中存放可查找的键及相应定位信息。真实页结构因数据库而异。',
        '查27时先判断它位于[20,40)，进入中间叶页。查[27,42]时先定位27，再沿叶子顺序读27、31、42，遇50超过上界停止。点击逐步演示，观察点查与范围查共享的起点定位，以及范围查多出的连续读取。',
      ],databaseFigure:'index'},
      {id:'composite-index',title:'联合索引先按班级，再按学号，意味着什么？',paragraphs:[
        '(class_name,id)可以理解为先按班级排序，在同班内部再按学号排序。条件同时给出一班和编号范围时，通常能够定位更紧凑的区间；只有学号时，匹配记录可能分散在不同班级的区域，不能照搬双条件的成本判断。',
        '但也不能把“没有最左条件”绝对化成任何产品都不可能使用索引。部分实现和数据分布可采用跳跃扫描等路径；最终是否选择、读取多少数据，需要看实际计划。教学顺序规则用于理解组织方式，不是替代优化器的永恒口诀。',
      ],code:{title:'联合索引与两种不同查询条件',text:"CREATE INDEX idx_students_class_id ON students(class_name,id);\nSELECT id,name FROM students\nWHERE class_name='一班' AND id>=102;\n-- 102 小周\nSELECT id,name FROM students WHERE id=102;\n-- 结果相同不意味着访问路径和成本相同"}},
      {id:'table-access',title:'索引里找到了编号，为什么还可能继续访问表？',paragraphs:[
        '如果索引中只有筛选键与定位信息，而查询还需要其他列，数据库通常仍需访问相应表数据。不同引擎中定位信息可能指向行位置或主键等，不能把某一种引擎的“回表”过程当成所有数据库的统一存储格式。',
        '索引覆盖所需列可能减少额外访问，但在PostgreSQL等系统中，能否真正避免堆访问还涉及可见性条件。返回很大比例的表时，连续扫描也可能比大量离散访问便宜。不要因为一次计划选择顺序扫描就断言索引失效或优化器有错。',
      ]},
      {id:'explain-cost',title:'读执行计划，先找行数估计与实际访问量',paragraphs:[
        'EXPLAIN展示计划，节点可能包含扫描、连接、聚合和排序。成本值通常是优化器的相对估计单位，不应直接读成毫秒。估计返回行数与真实数据分布相关，统计信息过时或字段相关性可能使判断偏离实际。',
        'PostgreSQL的EXPLAIN ANALYZE会实际执行语句。练习中只对可控的SELECT使用它，并关注实际行数、循环次数、过滤量和时间；不要在真实业务库里随意对UPDATE或DELETE加ANALYZE。几行样例适合验证结果，不适合证明百万行性能。',
      ],code:{title:'PostgreSQL只读计划观察示例',text:"EXPLAIN SELECT student_id,score\nFROM enrollments WHERE course_id='C1';\n-- 在可控练习环境再观察实际执行：\nEXPLAIN (ANALYZE, BUFFERS)\nSELECT student_id,score FROM enrollments WHERE course_id='C1';"}},
      {id:'practice-evidence',title:'一次完整优化实验，怎样避免“看起来变快了”？',paragraphs:[
        '先保存查询、结果校验方式、数据规模和基线计划，再提出明确假设，例如“按课程查询很常见，现有联合主键却先按学生组织”。在独立实验环境增加候选索引，使用相同参数对比执行计划与结果，确认没有通过少返回数据换取所谓提速。',
        '重复测试并说明缓存冷热与并发负载，记录读收益和写入代价。若优化无收益，应保留证据并重新判断瓶颈，不必强行留下索引。最终报告应能回答：改了什么、为什么预期有效、实际发生什么、在哪些条件下结论成立。',
      ],check:{question:'四行样例里建索引后仍使用顺序扫描，能证明索引没有用吗？',answer:'不能。小表扫描可能很便宜，优化器未必选择索引。要用符合目标分布和规模的数据、相同查询结果及可比较的测量条件，再判断读收益与维护代价。'}},
    ],recap:['索引是一种有维护成本的访问路径，不能替代正确的查询语义。','理解键顺序、范围定位及额外表访问，才能解释计划选择。','性能结论必须有数据规模、执行计划和可复现测量作依据。'],sources:[indexes,explain,{label:'PostgreSQL：覆盖索引与可见性检查',href:'https://www.postgresql.org/docs/18/indexes-index-only-scans.html'}],
  },
}
